# Decisions

One line per decision made where the spec was silent, ambiguous, or (rarely) contradicted itself. Newest at the bottom, grouped by phase.

## Phase 0

- **Repo location.** The session's shell started in `/Users/seungmin` (the whole home directory, already an empty untracked git repo — not something to build into) and later in `/Users/seungmin/Documents/trading-journal`, an existing, unrelated trading-journal app (Prisma/Express/Docker) that `docs/build-prompt.md` §0 explicitly says not to reference or reuse. Created a fresh repo at `/Users/seungmin/Documents/min-journal` instead. **Confirmed correct by the user.**
- **Home-directory git repo removed.** `/Users/seungmin/.git` (zero commits, plain `git init` output, predates this session) was deleted at the user's request — a repo rooted at the whole home directory was flagged as a future hazard regardless of who created it.
- **`docs/` folder name.** The handed-off bundle's docs folder was named `docs:` (a colon, not a slash) on disk — an extraction artifact, not a spec choice. Copied its contents into `docs/` (no colon) in the new repo.
- **Storage architecture — README vs. build prompt.** `docs/README.md` (Overview, and "State Management" → Storage) recommends a local-first SPA persisting to IndexedDB, explicitly out of scope for sync in v1. `docs/build-prompt.md` §2 locks in Next.js 15 + Supabase (Postgres + Auth + Storage). **Confirmed by the user: Next.js + Supabase, final.** Reason given — the phone needs direct access, and chart screenshots must be shared between PC and mobile (a local-only store on one device can't do either). The README's local-first paragraph is superseded and ignored; everything else in README (screens, copy, tokens, entity shapes) still governs.
- **Next.js version.** `create-next-app@latest` scaffolds Next 16 today; pinned `next` and `eslint-config-next` to `15.5.25` (latest stable 15.x) to match the locked spec.
- **ESLint flat-config bridge.** `eslint-config-next@15.x` still ships the legacy eslintrc format (its native flat-config export ships in v16), so `eslint.config.mjs` bridges it with `@eslint/eslintrc`'s `FlatCompat`, loaded via `createRequire` (Next's internal lint step doesn't reliably follow the package's ESM `exports` condition). Routine compatibility plumbing, not a design decision — noted here in case it looks surprising in review.
- **Pretendard delivery.** Self-hosted the **variable** font file (`pretendard/dist/web/variable/pretendardvariable.css`, ~2MB, one file covering the full weight axis) rather than four separate static weight files (~3MB combined for 500/600/700/800). Same visual result, smaller download.
- **Western P&L convention colors.** `docs/README.md` deliberately leaves these unspecified ("expose the convention as a setting... rather than hardcoding" — no hex given). Chose gain `#22c55e` / loss `#f04452` (reusing the existing KR-gain red for the western loss role, since western loss=red is the same hue already in the palette). Both pairs live behind CSS variables (`--pnl-gain` / `--pnl-loss`, swapped by `[data-pnl="west"]` on `<html>`), so this is a one-line change if the user has a different pair in mind.
- **Tailwind theme keys for half-step sizes.** CSS custom-property names can't contain a bare `.`, so the type scale's half-steps (11.5, 12.5, …) are keyed as `--text-11_5` etc., producing utility classes like `text-11_5`. Verified these actually emit CSS (not just silently dropped) via the production build output.

## Phase 1 — resolved by the user

- **`Trade.confirmation`.** Free text, single line. Lives in New Trade §3 Execution, directly under the "Entry model" select. Read-only, shown alongside the other CRT-sequence chips on Trade detail. Not a select — the option set isn't settled yet. `trades.confirmation` is a nullable `text` column, no constraint.
- **HTF pairing.** Fixed select, exactly four options, in this order, default **W → 2D**:
  1. `M → W → 2D`
  2. `W → 2D`
  3. `D → H1`
  4. `H1 → M5`
  Stored as short codes (`m_w_2d` / `w_2d` / `d_h1` / `h1_m5`) on `trades.htf_pairing`, display strings resolved in the UI layer (same pattern as `session`) — keeps a `CHECK` constraint cheap to extend later and keeps the arrow glyph out of the database.
- **Instrument.** A combobox: free text + a preset list. No DB constraint (free text stays allowed) — the preset list is UI-only data. `settings.default_instrument` defaults to `EURUSD`. **The list below was superseded in Phase 4a** — see the entry at the end of this file for the final 28 and the dropped `XAUUSD`. Originally:
  - Majors (7): `EURUSD`, `GBPUSD`, `USDJPY`, `USDCHF`, `USDCAD`, `AUDUSD`, `NZDUSD`
  - Crosses (21): `EURGBP`, `EURAUD`, `EURNZD`, `EURCAD`, `EURCHF`, `EURJPY`, `GBPAUD`, `GBPNZD`, `GBPCAD`, `GBPCHF`, `GBPJPY`, `AUDNZD`, `AUDCAD`, `AUDCHF`, `AUDJPY`, `NZDCAD`, `NZDCHF`, `NZDJPY`, `CADCHF`, `CADJPY`, `CHFJPY`
  - Plus `XAUUSD`
  - This adds a **Combobox** primitive to Phase 3's component list (not in the build prompt's original primitive list) — free-text input + filtered preset dropdown, same field styling as Select/Input.
- **Western P&L colors.** Confirmed as-is (`#22c55e` gain / `#f04452` loss) — real usage is Korean convention only, not worth further design time.

## Phase 1 — additional decisions

- **`settings` row provisioning.** Nothing in the spec says how a user's `settings` row comes to exist (there's no sign-up flow to attach it to). A Postgres trigger on `auth.users` insert (`handle_new_user`) seeds one default `settings` row (`pnl_convention='kr'`, `default_instrument='EURUSD'`, `default_session='asia'`, `r_precision=1` — `locale` was part of this row too until it was dropped, see the Phase 3 follow-up entry below). `accounts` are **not** auto-seeded the same way — starting capital, currency, and risk mode need real user input, so the first account is created through onboarding UI (Phase 8), not a trigger default.
- **`session` default.** README doesn't state one; picked `asia` (the first session of the trading day) as `default_session`'s seed value. Easy to change in Settings once that screen exists.
- **`r_precision` default.** Not specified; seeded at `1` (one decimal place on R values, matching every R value shown in the mocks, e.g. "+18.4R").
- **Storage bucket RLS.** `chart-shots` is private; objects are scoped by putting the owning user's id as the first path segment (`{user_id}/{trade_id}/{filename}`) and writing storage RLS policies against `auth.uid()::text = (storage.foldername(name))[1]` — the standard Supabase per-user-folder pattern, since `storage.objects` has no `user_id` column of its own to key RLS on directly.
- **`CHECK` constraints, not native Postgres enums, for `direction` / `session` / `htf_pairing` / `result` / etc.** Keeps future value changes a plain migration (`ALTER TABLE ... DROP/ADD CONSTRAINT`) instead of `ALTER TYPE` ceremony (which can't drop values). Trade-off: `supabase gen types` can only narrow to a literal union from a real Postgres enum, so these columns come back as plain `string` in `database.types.ts`. Tighter literal-union types + zod schemas for these fields belong in `src/lib/domain/` once that lands in Phase 2 — the generated file is intentionally the loose, raw-schema layer, not the app-facing one.
- **Migrations validated locally before handoff.** Ran `pnpm exec supabase start` (local Docker Postgres + auth + storage) once, applied all four migrations against it end to end with no errors, generated `src/lib/database.types.ts` from that local instance, then `supabase stop`.
- **Login screen has no mockup** (`docs/README.md` § Routes lists `/login` with none). Built plain with tokens directly rather than Button/Input primitives, since Phase 3 (design system) hadn't landed yet. **Refactored onto `Field`/`Input`/`Button` at the end of Phase 3**, as flagged here.
- **Bug found + fixed: `loginSchema` couldn't be exported from `actions.ts`.** That file is `"use server"`, which only supports async-function exports (server actions) — re-exporting the zod schema object from it corrupted the object across the server/client bundle boundary, and `zodResolver` threw `Invalid input: not a Zod schema` at runtime. Moved `loginSchema`/`LoginInput` into `src/app/login/schema.ts` (no `"use server"`), imported by both `actions.ts` and `login-form.tsx`. Caught by actually loading `/login` in a browser, not by `pnpm build` (Next's prerendering never executes the client component's `zodResolver()` call, so the build stayed green through this).

## Phase 1 — applied to the real project and verified live

Cloud project created (`ap-northeast-2`, Seoul), `.env.local` filled in, one account created manually via the dashboard.

- Pushed all 4 migrations (+ a 5th, below) via `supabase db push --db-url ...` against the **regional pooler** host (`aws-0-ap-northeast-2.pooler.supabase.com`) — the direct host (`db.<ref>.supabase.co`) is IPv6-only and unreachable from this network. `supabase link`/`login` need a personal access token we don't have, so every CLI operation here goes through `--db-url` instead (see `docs/setup.md`).
- **Backfill migration added:** `20260909140500_backfill_existing_user_settings.sql`. The manually-created account predates the `handle_new_user` trigger, so it had no `settings` row — confirmed missing by querying `auth.users left join public.settings`, then wrote an idempotent backfill (rather than just fixing it by hand) so the migration history matches reality and any future fresh environment ends up consistent.
- **Verified directly against the live DB** (via `psql` over the pooler, `libpq` installed with `brew install libpq` since no Postgres client was present): all 8 tables exist; RLS is `t`/`f` (enabled, not forced) on all 8; exactly one `owner_only` `ALL` policy per table with the expected `auth.uid() = user_id` qual/check; `chart-shots` bucket exists (`public=false`) with all 4 object policies; `on_auth_user_created` trigger exists and is enabled; the real account now has its `settings` row (post-backfill).
- **Verified login end-to-end through the real page**, not just the API: created a disposable throwaway user via the Admin API (never touched the user's real password), drove `/login` in an actual browser — filled the form, submitted, confirmed the redirect to `/`, confirmed the session persists on a second protected route (`/dev/tokens`, no bounce to `/login`), confirmed visiting `/login` while authenticated redirects back to `/`. Deleted the throwaway user afterward (Admin API) and confirmed via SQL that its `settings` row cascaded away and the real account was untouched.
- Regenerated `src/lib/database.types.ts` from the live project's `--db-url` — byte-identical to the local-instance version generated in Phase 1's first pass, confirming the two schemas match exactly.

## Phase 1 — bug found by opening `/login` in a real browser: Tailwind spacing was 4x too large everywhere

The user reported `/login` rendering broken — tall narrow card, square inputs, a button that didn't stretch, system font instead of Pretendard. `pnpm build`/`lint`/`test` were all green throughout; none of them would have caught this.

- **Root cause:** Tailwind's default `--spacing` base unit is `0.25rem` (4px) — bare numeric spacing utilities (`p-32`, `gap-16`, `w-32`, `px-16`, ...) compute to `N * 4px`, not `N px`. Every spacing value in `docs/README.md`'s Design Tokens is already a literal px number, and every component written so far (`/login`, `/dev/tokens`, `/`) used those numbers directly as Tailwind class suffixes (e.g. `p-32` meaning "32px"), assuming a 1:1 mapping that Tailwind does not use. Confirmed in a real browser via computed styles: `p-32` on the login card computed to `padding: 128px`, not `32px` — 4x over, exactly matching the multiplier.
- **Why colors/radius/font-size looked fine:** those come from custom `@theme` keys with fixed, explicit px values (`--radius-24: 24px`, `--text-56: 56px`, ...), not from Tailwind's dynamic spacing-multiplier scale — only the spacing-derived utilities (padding/margin/gap/width/height/inset) were affected.
- **The font was not actually broken** — `document.fonts` showed `"Pretendard Variable"` loaded and applied on every element checked. The 4x spacing bug made the layout collapse enough that Pretendard's fairly plain Latin letterforms read as a generic system font at a glance; there was no separate font-loading bug to fix.
- **Fix:** redefined `--spacing: 1px` inside the `@theme` block in `globals.css`, rather than rewriting every call site to the `N/4` form (verified first that the codebase has zero existing usages of Tailwind's fractional spacing convention like `p-1.5`/`gap-2.5`, so there was nothing to conflict with). One-line change, fixes every page at once.
- **Verified the fix in a real browser, not just a rebuild:** re-inspected computed styles on `/login` (card padding now `32px`, email input width now `316px` = 380 − 2×32, button padding `17px`), then logged into `/dev/tokens` and `/` with a second disposable test user (created and deleted the same way as the Phase 1 login test) and visually confirmed every section — colors, type scale, radius swatches, spacing bars — renders correctly, not just the one page that was reported broken.
- Fixed a stale, now-wrong claim in `/dev/tokens`'s own copy ("Tailwind default scale — 4px base already matches") to describe the actual `--spacing: 1px` override instead.

## Phase 2 — domain logic

Four rules the spec left open, put to the user and confirmed before implementing:

- **Break-even R.** `result` is a label, never an input to the R maths: `realizedR` always comes from the actual exit, so a 'be' trade lands at ~0.0R on its own instead of being forced to exactly 0. Keeps `pnlAmount` (and therefore the balance series) reconcilable with the real account when a "break even" exit was a few ticks off. **No exit → `realizedR` is `null`, not 0**, and such trades are skipped by the aggregates rather than counted as zeros (counting them would quietly drag net R and expectancy toward zero).
- **Break-even in the aggregates.** Win rate = wins / trades-with-a-result, so BE sits in the denominator only — mock 2c prints "67%" with the sub-line "6 / 9", i.e. the denominator is the trade count. Avg win / avg loss exclude BE entirely. Expectancy includes it (a scratch really does dilute expectancy). **Win streaks treat BE as neutral** — it neither extends nor breaks a run, it is skipped; only a loss resets. A trade with no result yet is skipped the same way.
- **Mid-range band = `MID_RANGE_HALF_BAND` (0.10), i.e. 40%–60%.** Symmetric around the 50% equilibrium line, the only range level `docs/README.md` actually draws. Sanity check: the one trade the canvas chips "On plan" (mock 2b) has its entry at 9.9% of the range, far outside the band. Kept as a named constant so it can move into Settings later.
- **`offPlan` is trade-local: `sweepSide === 'none' || entryIsMidRange`.** Model retirement is deliberately *not* an input. `offPlan` is recomputed on every read, so including model status would retroactively flip already-logged trades to off-plan the moment a model is retired — silently rewriting history and moving past adherence numbers, which a trading journal must never do. README §Playbook ("retiring ... marks future matches off-plan") and the retired model's mock description ("new entries are flagged off-plan") are therefore read as applying **only to trades recorded after the retirement**, honoured as a warning in the trade form at log time. The mock stays consistent either way: its 8 off-plan trades all belong to the retired "Mid-range entry" model, whose own description is "No sweep, no confirmation" — so they are already off-plan via `sweepSide === 'none'`.
- **Rule adherence = (trades − off-plan trades) / trades**, over every trade in the period (BE included), reading the derived `offPlan` flag — *not* the manual "On plan" tag, which is a separate self-assessment tag sharing a row with Impatient / Chased entry / Early exit. Not a judgement call: reverse-engineered from two independent mocks and confirmed. Weekly 2c shows 89% with "1 off-plan" over a 9-trade week ((9−1)/9 = 88.9%); dashboard 1a shows 87% over 61 trades (24+18+11+8, matching "View all 61") of which the 8 "Mid-range entry" ones are flagged ((61−8)/61 = 86.9%). Both are locked in as regression tests.
- **Time-weighted return is chain-linked.** The timeline is cut into sub-periods at every cash movement; each sub-period returns (trading P&L / balance at the start of that sub-period); the factors are multiplied. Sub-periods starting at a balance ≤ 0 are skipped rather than dividing by zero; no trades at all → 0%. **Mock 3a's "Return on capital +43.0%" is not a TWR** — it is the same number as the "+43.0R lifetime" printed beside it, i.e. the plain sum of R at 1% risk, and was judged to be the mock author reusing the R figure rather than specifying the maths. We compute the real thing, which is what README's own "time-weighted, so deposits don't inflate it" asks for. Covered by a test where TWR is 21% while the naive money-weighted view would read 15.5%.

Smaller calls made while implementing, none of them spec'd:

- **Same-date event ordering: cash movements before trades**, `createdAt` as the final tiebreak — a deposit dated D funds that day's trading. Makes the balance series, the TWR sub-periods, and the ledger's running-balance column all deterministic.
- **"Win streak" is ambiguous in the mock** (dashboard shows "4 trades" with no sub-line, so it could be the current run or the best one). `winStreaks` returns **both** `longest` and `current` rather than guessing. **The dashboard shows `current`** — decided by the user: a streak is a read on the state you are in right now, not a personal-best record. `longest` stays on the selector (it costs nothing and the weekly review may want it) but is not on any screen.
- **Selectors return raw ratios (0..1), never pre-rounded percentages.** Display rounding (adherence to a whole %, R to `settings.r_precision`) belongs to the UI, so one selector can feed both a stat card and a chart. Tests assert both the raw value and the rounded one the mock shows.
- **Domain types are camelCase and carry real literal unions**, bridging the loose generated `database.types.ts` (where CHECK-constrained columns are plain `string`). The row → domain mapper lands with the queries in Phase 4.
- **Dates are compared as `YYYY-MM-DD` strings**, never `Date` objects — lexicographic order is chronological order and there is no timezone to get wrong.
- **Currency is left at full float precision in the domain**; rounding is display-only. Tests compare money to the cent (`toBeCloseTo(x, 2)`) since e.g. 9.68R × $1,000 lands on 9680.000000000002.
- **Drawdown warning margin is 2 *percentage points*** (`DRAWDOWN_WARNING_MARGIN_PCT`), so a 10% limit starts warning at 8% drawdown — the natural reading of README's "within 2% of the limit". Cross-checked against the mock: peak $32,180 with a −10% limit gives "Stop trading at −$3,218".
- **Mock 2b's "93% captured" is a rounding artifact**, like the 43.0% above: 2.8103R / 3.0R is 93.7%. Both displayed numbers in the mock are themselves rounded, so there is no real conflict — `captureRate` computes from raw values.

## Phase 3 — design system primitives

- **`clsx` + `tailwind-merge` added.** Not a component kit (the build prompt bars those) — they are the class-merging utility that lets a caller's `className` actually override a component default, which plain string concatenation cannot do (CSS source order decides, not string order).
- **tailwind-merge had to be taught this project's scales, and silently corrupted components until it was.** Our type scale is keyed by px number (`text-13`, `text-11_5`), which collides with Tailwind's text-*colour* namespace: out of the box tailwind-merge classified `text-13` as a colour and dropped the earlier `text-white`, so the `ink` button rendered as **black text on a black fill**. The px-named radius scale had the same problem in reverse (`rounded-24` and `rounded-8` never deduped). `src/lib/cn.ts` now extends the merge config with both scales. Caught by looking at `/dev/components` in a browser — `pnpm build`, `lint`, and `tsc` were all green with the bug in place, which is the same shape of failure as the Phase 1 zodResolver bug and the Phase 2 spacing bug.
- **The capital chart's 1R line gets its own vertical band** (viewBox y 62%–89%, matching the mock's 106–152 of 170) rather than being stretched over the full height. 1R is a fixed percentage of the balance, so on a full-height "own scale" it normalises to the *exact same shape* as the balance line and disappears underneath it — which is precisely what happened on first render. Confirmed against the mock rendered in a browser, where the 1R line clearly sits lower and flatter than the balance line.
- **`BottomTabBar` is designed, not mocked.** The 1d mobile mock has no tab bar (its home screen ends at the "기록하기" CTA), but the build prompt requires one for mobile nav. Built from the TopBar's vocabulary — active takes ink, inactive stays muted — with every target over the 44px minimum and `env(safe-area-inset-bottom)` padding.
- **`RangeDiagram` is built with divs, in Phase 3**, per the build prompt's note that it is layout rather than a chart. Vertical position is price (top edge = range high, bottom = range low, hairline = 50% equilibrium); horizontal offsets are reading order only and match the mock's. The "Sweep" label landing on the same line as the caption row was checked against the canvas rendered in a browser — the mock does the same thing, so it is faithful rather than a bug.
- **`EmptyState` is designed, not mocked**, as docs/README.md § Empty states asks: the card vocabulary at rest, a 700 16px title over a 500 13.5px line, with one optional action.
- **Charts reproduce the mocks' `preserveAspectRatio="none"`**, so strokes stretch horizontally with the container exactly as the mock does. That is deliberate fidelity, not an oversight.

## Phase 3 follow-up — mock-vs-gallery audit + viewport locale

The user opened `/dev/components` next to `docs/design-canvas.html` and flagged four things to check against the mock's real values rather than eyeballing. All four were checked by reading the canvas's inline styles directly, three needed a real fix.

### 1. "Recent trades" tiles truncating

Root cause was a **gallery demo layout bug, not a primitive bug**: the Entry/Stop/Exit/Hold tiles (actually Trade Detail's Result-card info tiles, mislabeled "Recent trades" in the gallery) were nested inside a 2-column `grid-cols-2` split shared with an EmptyState demo, leaving each tile ~84px wide against 17px bold text like "23,396.50". The mock's real layout is `grid-template-columns: 220px 1fr` (Realized value | tiles) inside its own full-width 1000px card, with the tiles at `repeat(4,1fr) gap:12`. Fixed by splitting the mislabeled block into two accurate, correctly-scoped demos:
- **"Recent trades row"** — the actual dashboard grid (`64px 1fr 150px 130px 110px 90px`, 16px gap, real mock row data), which the gallery had never actually demonstrated.
- **"Result info tiles"** — the real `220px 1fr` split with `repeat(4,1fr)` tiles, at full card width. No truncation at any size checked (verified via `scrollWidth`/`clientWidth` in the browser, not eyeballed).

### 2. Letter-spacing / line-height / gaps reading wider than the mock

Three real, verified discrepancies, all against a spec that turned out to have more variants than the primitives modeled:

- **StatCard had an invented 28px size that doesn't exist in README's own type scale.** The canvas mock does use 28px in one place (Capital's 4-up row), but `docs/README.md`'s Design Tokens § Typography lists the scale explicitly and 28 is not in it (11/11.5/12/.../26/30/44/56). Per `docs/build-prompt.md` §1's rule ("README와 캔버스가 충돌하면 README를 따르고"), rounded Capital's stat cards to the 30px family used everywhere else (dashboard, weekly review) rather than adding a one-screen-only size. Also found: **the calendar summary row's 26px values carry no letter-spacing at all** in the canvas markup, while every 28px+ value explicitly sets `-0.03em` — an intentional-looking distinction, not an oversight, so `StatCard` now has exactly two variants (`sm`: 26px, no tracking; `lg`: 30px, `-.03em`) instead of one shared tracking class.
- **`BarRow`'s caption line used `margin-top:8px`; the mock uses 6px.** The bar's own margin-top does equal its track height (8px for an 8px track, 7px for a 7px track — that part was already right), but the caption below it is a separate, smaller gap. Also found the 7px-track variant (weekly tag frequency) uses a **13.5px label**, not 14px — the two track heights are genuinely different components sharing a shape, not one component at two sizes. `BarRow` now branches its label size and bar margin off `height`, with the caption fixed at 6px regardless.
- **The CRT-sequence chip row (trade detail) uses a 10px gap; the emotion-tag row (new trade form) uses 8px.** The gallery had both using 8px, having conflated two visually similar but differently-specified pill rows. Demo split to show both gaps explicitly rather than picking one.

### 3. Segmented's Win state — confirmed intentional, left as-is

Checked the canvas's raw markup for the Result segmented control directly: `Win` selected is `background:#fdeced` (`--pnl-gain-tint-1`) with `color:#f04452` (`--pnl-gain`) — pixel-exact match to what was already implemented (`tone: "gain"`). This is deliberate per `docs/README.md` § New trade itself ("the selected Win state is #f04452 on #fdeced"): Result is the one segmented control in the whole app whose selected state carries P&L meaning rather than the neutral accent-blue selection every other segmented control (including Session) uses. No code change — this entry is the record the user asked for.

### 4. Other mismatches found while auditing (not separately requested, fixed alongside)

Nothing further turned up beyond the above three fixes — the rest of the primitives (Card, Chip base shapes, RangeDiagram, charts) were re-checked against the same canvas sections and matched.

### 5. Language is decided by viewport width, not `settings.locale`

Confirmed rule: **≥900px → English** (matches the responsive breakpoint already used for layout — docs/README.md's own `<900px` language), reading the PC mocks (1a-1c, 2a-2d, 3a); **<900px → Korean**, reading the 1d / 3b-mobile mocks. Reasoning given: mobile is for logging a trade in the moment (native language reads faster under time pressure), desktop is for analysis (trading terms read more naturally in English, which is also what every PC mock is authored in). Not a user setting — the same one responsive codebase now branches copy the same way it already branches layout, with no `/mobile` route.

**Mechanism — `src/lib/i18n/` (`locale.ts` + `locale-context.tsx`):**

- `getViewportLocale(width)` is the single source of truth for the breakpoint (900px, `LOCALE_BREAKPOINT_PX`, deliberately identical to the layout breakpoint already in the responsive rules — one number, not two that could drift apart).
- **No-flicker strategy, and why it doesn't need a blocking `<head>` script:** the classic "avoid flash" pattern (à la `next-themes` for dark mode) uses a blocking inline script to mutate the DOM before React ever mounts, because that trick is for a CSS class toggle — trivial vanilla-JS DOM work with nothing to reconcile. This is different: the payload is *text content*, which has to go through React's tree regardless (there is no CSS-only way to swap arbitrary Korean/English strings without rendering both languages into the DOM at once, which does not scale as the app's i18n mechanism for every future screen's copy — see the tradeoff note below). So instead:
  1. **`RootLayout` (Server Component) reads the `viewport-locale` cookie** (`cookies()`, Next 15's async API) and renders `<html lang>` and the `LocaleProvider`'s starting state from it. Once a visitor has been here before, this is correct from the very first byte the server sends — no client correction ever runs.
  2. The **only** time the guess can be wrong is a visitor's first-ever request, when no cookie exists yet — the server then defaults to `"en"`. `LocaleProvider` initializes its React state to that exact server-provided value (required: the client's first render must match the server's or React throws a hydration-mismatch error), then in a **`useLayoutEffect`** — which fires synchronously after the DOM commits but strictly *before the browser paints* — measures the real `window.innerWidth` and corrects the state if it disagrees, and writes the cookie for every future visit. Because this runs before paint, the correction is invisible; there is nothing to see flash.
  3. That same effect attaches a `matchMedia('(min-width: 900px)')` listener, so live resizing across the breakpoint (a desktop window resize, a tablet rotation) updates the locale immediately with no reload.
  - **Verified live in a browser**, not just read from the code: resized an actual window from 1400px to 700px and back. At 700px, `document.cookie` showed `viewport-locale=ko`, `document.documentElement.lang` was `"ko"`, and both `TopBar`'s and `BottomTabBar`'s nav labels (pulled from the *same* shared dictionary via `t()`) switched to Korean live, with no reload; resizing back to 1400px flipped both back to English live. (This sandbox's browser window doesn't let its CSS viewport track `resize_window` reliably — the first couple of attempts read back the old width — so this was confirmed by re-reading `window.innerWidth` after the resize each time until it actually reflected the new size, not by trusting the resize call alone.)
- **A real bug this caught that `pnpm build`/`lint`/`tsc` all missed**, the same shape as the Phase 1 `zodResolver` bug and the Phase 3 `tailwind-merge` bug: `parseInitialLocale` was originally defined in `locale-context.tsx`, which is a `"use client"` module. Next's RSC compiler does not allow a Server Component to call a plain function imported from a `"use client"` file directly (only render it as a Component, or pass it down as a prop) — `RootLayout` calling it threw `Attempted to call parseInitialLocale() from the server` at request time, a 500 on every single route, invisible to any static check because the type signatures are fine and nothing about it is wrong until it actually executes on a real request. Fixed by moving the function into `locale.ts` (no `"use client"`), which is safely importable from both sides.
- **Side effect: every route is now dynamically rendered** (`ƒ` instead of `○` in the build output), because `RootLayout` calling `cookies()` opts the whole tree out of static generation. Judged a non-issue here: `src/lib/supabase/server.ts`'s `createClient()` already calls `cookies()` on every route that touches the user's own data, so essentially every real screen (Phase 4 onward) was always going to be dynamic regardless — this only newly affects today's placeholder/dev-only routes (`/`, `/dev/tokens`, `/dev/components`), which aren't representative of the app's final shape. Worth knowing, not worth working around, for a single-user authenticated app with no public/SEO surface.
- **`useT()` takes the `{ en, ko }` pair inline at the call site** (`t({ en: "Dashboard", ko: "홈" })`) rather than a centrally pre-registered key path, per `docs/build-prompt.md` §9 ("타입 세이프한 사전 객체 + useT() 훅으로 충분히 구현한다") — no screen's real copy exists yet beyond what Phase 3 touches (the login screen, which has no mock either — both locales' copy there are original, chosen to read naturally per docs/README.md §9's own example of non-literal pairing), so there is nothing to key yet. A centralized dictionary can replace this later if duplicate pairs across call sites become a real problem; noting the option here rather than building it before it's needed.
- **Zod validation messages are locale-aware too, client-side only.** `schema.ts` exports `createLoginSchema(locale)`; the client (`LoginForm`) builds it from `useLocale()` so field errors read in the same language as the rest of the screen. The server action's defense-in-depth re-validation has no viewport to read, so it always uses the English default — that path only surfaces if JS is disabled or the request was malformed, not a real user-facing case.

**`settings.locale` — dropped.** A manual-override reinterpretation was proposed here (nullable, or a new `'auto'` value alongside `'ko'`/`'en'`) but rejected by the user: viewport decides language, full stop — no override means no column, and an unused setting left sitting in the schema just invites confusion later about why it's there. `20260909170000_drop_settings_locale.sql` drops it; `handle_new_user`'s `insert into public.settings (user_id) ...` needed no change (it only ever relied on column defaults). Applied to the live project and verified directly via `psql` — the column is gone, the existing settings row survived untouched on its other columns, and `database.types.ts` was regenerated to match.

## Phase 4a — New trade form

Four open questions were put to the user before building; all four confirmed, with two adjustments noted below.

**The mock's form is incomplete relative to its own data model.** Mock 1b has exactly 13 labelled fields (Instrument, Date, Session, HTF bias, HTF pairing, Range high, Range low, Entry, Stop, Target, Size, Entry model, Result) and **no Exit input** — yet `realizedR = (exit − entry) / risk`, so a trade logged from those fields alone produces no R at all, and every R on the dashboard, calendar, equity curve and Capital screen would be blank. Trade detail (2b) meanwhile displays Exit, Hold, Exit reason and "93% captured". README's own entity list and the build prompt's SQL both carry `exit`/`exit_reason`/`hold_minutes`. Applying `docs/build-prompt.md` §1 (README wins over the canvas), **Exit was added to the Execution section** as an optional field. Exit reason and hold minutes belong to Trade detail's edit path in 4c.

- **Required (blocks save) = exactly the NOT NULL columns the user must supply**: instrument, date, direction, session, htf pairing, range high, range low, entry, stop, size. README's own shorter list ("instrument, date, entry, stop") predates the schema; the extra four are all columns the database refuses to accept as null.
- **An open trade saves.** Exit and result are both optional and independent. A trade with no exit is simply still running: `realizedR` is null, so Phase 2's selectors skip it for net R and expectancy while it still counts toward win rate if a result was set. That asymmetry is deliberate and was confirmed in Phase 2.
- **`result` is auto-suggested from the exit's sign, then locked.** Once the trader picks a result by hand, a `resultTouched` flag stops the suggestion from ever overwriting it again — including on later exit edits. **Adjustment requested by the user**, and the reason is sound: the app must never silently overrule a judgement the trader has already made.
- **Blocking stops where the database stops**: stop ≠ entry, range high > low, size > 0, and unparseable numbers. Everything softer is a warning rendered under the form that never disables the save — target on the same side of entry as the stop, entry outside the range, off-plan, a retired model, a result that disagrees with the exit, and drawdown within 2 points of the limit. **A future-set date was dropped from the warning list at the user's request**: it's a rare slip, and a form that cries wolf gets all its warnings ignored.
- **Sweep side is derived from the stop, with an override.** `deriveSweepSide`: stop below the range low → low purged, above the high → high purged, inside → no sweep. README says it's "inferred from which extreme was purged" but never from what; the Playbook's own C2 rule 4 ("Stop beyond the sweep wick") does. Verified against the mock's own numbers — stop 23,396.50 against a 23,402.75 low derives "low", exactly the "Low purged" the mock prints. `both` is unreachable by inference, so the panel offers an explicit override.
- **`size` stays required and is record-keeping only.** It feeds no calculation (`pnlAmount` is `realizedR × rValueAtEntry`), but the column is already NOT NULL, a journal entry without position size is half a record, and it's known the moment the order is placed. Labelled in lots, since the confirmed instrument set is 28 FX pairs plus XAUUSD.
- **`rValueAtEntry` is computed, read-only, and recomputed server-side on submit.** The form shows "1R today · $X — frozen onto this trade when you log it"; the value the row actually stores is recalculated in the server action from the account and its ledger, never taken from the client. Hand-editing it is deliberately impossible: it would break the invariant the whole Capital screen rests on and silently desync money from R. (Phase 6's CSV import will need a per-row value, since historical trades' 1R can't be derived from today's balance — flagged for then.)
- **No account → a first-run setup sheet, not a guessed default.** `accounts` was genuinely empty, and `trades.account_id`/`r_value_at_entry` are both NOT NULL, so this is the real first-run path rather than an edge case. Auto-creating an account with an invented starting capital was rejected outright — that number drives every figure on the Capital screen. The sheet asks only what a trade needs (name, starting capital, risk %, drawdown limit), previews the resulting 1R, and Phase 8's Capital screen will edit this same row rather than create a competing one. Closing it returns to the dashboard.
- **Models select stays optional** with a "No models yet — add them in Playbook" empty state; trades without a model group under "Unassigned", which Phase 2's `byModel` already handles.

Verified end to end against the mock's own trade in a real browser: range size 83.50, sweep side "Low purged", 50% level 23,444.50, R:R "1 : 5.2R" and realized "+2.8R" all match mock 1b/2b exactly. Then checked the two paths that matter most — a closed trade stored `r_value_at_entry` 150 (15,000 × 1%), and a second, open trade stored exit and result as NULL with `r_value_at_entry` 154.22, correctly recomputed from the balance the first trade had moved. Test account and user deleted afterwards.

**Two bugs in the Phase 3 `Modal` primitive that only real interaction exposed**, both invisible to build/lint/tsc and to the Phase 3 gallery check:

- **Focus theft ate keystrokes.** `Modal`'s setup effect listed `onClose` in its dependencies, and every caller passes an inline arrow, so the effect re-ran on every parent render and called `panelRef.current.focus()` — pulling focus out of whatever field was being typed into. Only the first character of any input inside a modal survived. Fixed by holding `onClose` in a ref so the effect depends on `open` alone.
- **The portal broke hydration.** `Modal` guarded its portal with `typeof document === "undefined"`, so the server rendered nothing while the client rendered the portal on its very first pass — a mismatch that made React discard and rebuild the subtree, wiping typed-in state with it. Fixed with a `mounted` flag so both first renders agree on null. The gallery never caught this because its modal only opens after hydration; the account gate renders open on first paint, which exposed it immediately.


## Phase 4a follow-up — session set and instrument list trimmed

- **`ny_pm` removed from the session set.** The user doesn't trade the New York PM session, so it's gone from the allowed values rather than left as an option nobody picks: `20260909180000_drop_ny_pm_session.sql` rebuilds both CHECK constraints that carried it (`trades.session` and `settings.default_session`). Done while both tables were still empty, so nothing needed remapping. This is exactly the case Phase 1's "CHECK constraints, not Postgres enums" decision was made for — a two-line `DROP`/`ADD CONSTRAINT` instead of `ALTER TYPE` gymnastics, and dropping a value is something an enum can't do at all. Verified against the live database that `ny_pm` is now rejected on insert. Sessions are `asia` / `london` / `ny_am`.
- **Instrument presets fixed at 28 pairs, `XAUUSD` dropped.** The list and its order were given by the user directly: the 7 USD majors, then the crosses grouped by leading currency (EUR, then the JPY crosses, then GBP, AUD, CAD, NZD). Plain symbols, no broker prefixes. Metals are off the list while the focus is the majors — free text still accepts anything, so typing `XAUUSD` by hand keeps working, and `settings.default_instrument` stays `EURUSD`. The `/dev/components` combobox demo now pulls the real list instead of its own hand-rolled subset, so the two can't drift.

## Phase 4b — draft autosave + attachment upload

Two questions were put to the user before building; both confirmed, with the debounce shortened from the original 3s proposal to 1.5s per the user's own reasoning (a draft write is cheap; the risk of losing the last few typed characters to a closed tab isn't worth trading for a slightly quieter network tab).

**Draft autosave.** Field edits debounce 1.5s after the last keystroke; attachment adds/removes bypass the debounce and save immediately (losing track of an upload is worse than an extra write). `docs/README.md` and `docs/build-prompt.md` both already say "restore on return", not "ask" — implemented as a silent, automatic restore, with the header showing "Draft restored · HH:mm" once (reusing the mock's own "Draft saved · HH:mm" slot and format) so the trader knows it wasn't a blank form, without a confirmation dialog. The draft is deleted on a successful trade save; closing the form without saving leaves it in place for next time, per spec. One judgement call not asked about: a **restored** draft starts with `resultTouched` already `true`, since there's no way to know whether the trader had manually set Result in the prior session — treating it as already-decided is the safer default, matching the same "never overwrite a deliberate choice" reasoning behind the touched-lock itself.

**Attachments — the orphan-file lifecycle.** `attachments.trade_id` is NOT NULL, so a DB row can't exist before a trade does, and `drafts` has exactly one row per user (`user_id` is the PK), which is what makes the whole design tractable:

- Uploads land immediately in one fixed temp folder, `{user_id}/drafts/`, direct from the browser to Storage (not proxied through a Server Action, so large files don't round-trip its body) — matching the "restore on return" promise for images too, not just text fields. The draft's `payload.attachmentPaths` is the list of what's there.
- Removing a thumbnail deletes the Storage object immediately and updates the draft.
- On a successful trade save, every draft-listed path is moved (`storage.move`, an atomic rename — nothing left behind to clean up) to `{user_id}/{tradeId}/{filename}`, an `attachments` row is inserted per file, and the draft is deleted. Best-effort per file: one failed move doesn't roll back the trade that already saved.
- **Reconciliation runs once per New Trade page load**: anything in the draft folder the current draft doesn't reference gets deleted. Not a cron job — since there's only ever one draft per user, the next visit to this page is always a safe moment to check. This is the closest thing to a guarantee against orphans without standing infrastructure: the only real gap is a trader who uploads to a draft and then never opens New Trade again, ever — accepted as a fine edge case for a single-user personal app, flagged in the original proposal.
- Limits (none given in the spec, proposed and confirmed): PNG/JPEG/WebP, 8MB per file, 6 per trade. Client-side validation gates the picker; the server re-checks all three independently, since a client-side cap is trivially bypassable.

**Verified against the live project in a browser, not just re-read from the code** — every step below was checked against the actual database/Storage state, not just the UI:
- Filled fields, waited past the debounce, confirmed `drafts.payload` held the typed values and `updated_at` matched the header's timestamp; reloaded the page and confirmed both the fields and the header ("Draft restored · HH:mm") came back.
- Uploaded a real (in-page-generated) PNG through the actual file input: confirmed it landed at `{user_id}/drafts/{generated-name}.png` in Storage and the draft's `attachmentPaths` updated immediately, ahead of the debounce.
- Manually planted an orphan object in the same user's draft folder (a file the draft didn't reference), reloaded the page, and confirmed the orphan was deleted while the referenced file survived untouched.
- Removed an attachment from the form and confirmed both the Storage object and the draft's path list were gone.
- Logged a trade with an attachment and confirmed: the `attachments` row pointed at the final `{user_id}/{tradeId}/{filename}` path, the file existed there and *nowhere else* (no leftover at the draft path — the move was genuinely atomic), and the `drafts` row was gone.
- All test data (a disposable account, trade, attachment, and draft) was created under a throwaway auth user and fully deleted afterward — the user's own account, trade, and settings were read but never modified.

**Noted, not fixed (out of scope for this task):** `formatPrice` is fixed at 2 decimal places, so an FX pair's range size (e.g. 1.2650 − 1.2600 = 0.0050) displays as "0.00" instead of a useful value. This predates Phase 4b (it's the same helper used for the index/futures-style prices in the mocks, which don't need more precision) and only surfaced now while testing with a real FX instrument. Flagged for whichever phase next touches price formatting.

## Phase 4 follow-up — instrument-aware price/pip formatting (was deferred above, fixed before 4c)

The user rejected deferring this: every one of the 28 instrument presets is an FX pair, so a fixed 2-decimal `formatPrice` made every price and range-size figure on the New trade screen meaningless. Fixed as a display-layer-only change — the domain layer (`rangeSize` etc.) keeps full float precision; nothing here rounds before the value is stored.

- **Decimal precision is instrument-aware, not global.** `src/lib/instruments.ts` adds `isJpyPair` (`instrument.trim().toUpperCase().endsWith("JPY")`), `priceDecimals` (3 for JPY pairs, 5 for everything else), and `pipSize` (0.01 vs 0.0001). This app is FX-only by design (metals were already dropped in the Phase 4a instrument-list decision above), so there's no third "unknown instrument" bucket to design for — the two-way split covers every preset and every free-typed pair as long as it's shaped like an FX symbol ending in the quote currency.
- **`formatPrice(value, instrument)`** now takes the instrument and looks up its decimals instead of hardcoding 2. Used for the three absolute prices in the range diagram (Low / 50% / High captions).
- **Range size switches to pips, not raw decimal.** The user's own framing — "0.0050 is harder to read than 5.0 pips" — was confirmed directly: a new `formatPips(value, instrument)` divides the difference by the instrument's pip size and returns one decimal place (e.g. "50.0", "0.5"). The caller wraps it in the usual `useT` label (`"50.0 pips"` / `"50.0핍"`), matching how every other unit label in this app is localized rather than baked into the formatter.
- **Verified live**, not just unit-tested: created a disposable test user, logged into the actual New Trade form, entered a EURUSD range (1.2605–1.2655) and confirmed the diagram read "Low 1.26050 / 50% 1.26300 / High 1.26550" with "Range size 50.0 pips" (not "0.01" or "0.0050"). Switched the instrument to USDJPY with a 156.275–156.325 range and confirmed it re-rendered to 3 decimals ("Low 1.261 / 50% 1.263 / High 1.266") with "Range size 0.5 pips". Logged the USDJPY trade and confirmed via a direct `psql` query against the live database that the stored `range_high`/`range_low`/`entry`/`stop` kept full precision (`1.2655`, `1.2605`, `156.325`, `156.275`) — proving the fix never touched what gets persisted. Test trade, account, and user deleted afterward.

## Phase 4c — Trade detail

Two questions were put to the user before building; both confirmed.

**Editable scope.** The user chose full-field editing over restricting the edit path to only `exitReason`/`holdMinutes` (the two columns Phase 4a deferred here): every field the New trade form can set is editable from Trade detail too, on the reasoning that a trader who mis-typed an entry price needs to fix it, not just the two fields 4a happened to leave out. **`rValueAtEntry` is not merely left out of the UI — it doesn't exist anywhere in the edit schema or the `updateTrade` server action**, so there is no code path by which an edit can touch the frozen 1R value, by construction rather than by convention.

- The New trade schema was split (`src/lib/domain/trade-schema.ts`) into the shared field shape and numeric-refinement rules, reused by both `src/app/trades/new/schema.ts` (unchanged behavior) and the new `src/app/trades/[id]/schema.ts` (adds `exitReason`, `holdMinutes`, both optional — `holdMinutes` must be a non-negative integer when present).
- `tradeToEditInput()` seeds the edit form from a stored trade. One subtlety: it does **not** always carry the stored `sweepSide` forward as `sweepSideOverride`. It only does so when the stored side disagrees with what `deriveSweepSide` would compute fresh from the stored range/stop (i.e. it really was an explicit override — the only way to reach `"both"`). Otherwise it's left `null`, so editing the range or stop keeps live-deriving the sweep side exactly like the New trade form, instead of every edited trade permanently "sticking" at whatever sweep side happened to be stored.
- `collectWarnings` (moved from `src/app/trades/new/warnings.ts` to `src/lib/domain/warnings.ts` since two routes now share it) runs unchanged in the edit form — same blocking/warning split as creation.
- Attachments are **not** part of the edit form's own save step — the Charts card's "Add"/remove works directly against the trade's real `{user_id}/{tradeId}/` folder (`src/app/trades/[id]/attachments-actions.ts`, `use-trade-attachments.tsx`) whether or not the CRT-field form is open, matching the mock's independent "Add" link. Unlike the New trade form's draft flow, there's no temp folder or reconciliation step to build — the trade already exists, so uploads go straight to their final path. Removal is matched by `storage_path` rather than the attachment's own id, since `uniqueAttachmentFilename` already guarantees uniqueness and the client never otherwise needs to learn the database-generated id.

**Delete.** Confirmed present in the mock (2b's header "Edit"/"Delete" pair) — not something to newly design, just implement. Two things that needed deciding, since the mock is a static image with no interaction states:

- **A confirmation modal was added before the delete actually runs**, since the mock can't show what a static screenshot doesn't specify and this action is irreversible. First attempt styled the confirm button red via `bg-loss` to read as a "danger" action — wrong on two counts, caught by looking at it in the browser rather than just the code: this app's Korean P&L convention maps `loss` to **blue** (`#3182f6`, the same hue as the accent color — see the Phase 0 "hue collision" note), so it rendered visually identical to an ordinary primary button; and reusing a P&L color for a non-P&L destructive action would have been a semantic conflict even if the color had looked different. There is no destructive/danger color in `docs/README.md`'s design tokens, so the confirm button is a plain primary (accent blue) action instead, consistent with every other confirm-style button in the app.
- **Deleting a trade explicitly removes its Storage objects before deleting the row.** `attachments.trade_id` cascades at the database level, but a cascaded DB row disappearing does not delete the Storage object it pointed at — Postgres has no reach into Storage. `deleteTrade` lists everything under `{user_id}/{tradeId}/` and removes it first, matching the no-orphans standard Phase 4b set for draft attachments.

**Verified live**, not just unit-tested: created a disposable test user, an account, and a full EURUSD trade through the real New trade form (range 1.2605–1.2655, entry 1.2620, stop 1.2600, target 1.2680, exit 1.2650) — confirmed on submit it now redirects to `/trades/[id]` (previously landed on the dashboard placeholder, since Trade detail didn't exist yet) and the detail screen rendered the exact expected math: Realized +1.5R, Planned 3.0R · 50% captured, Range size 50.0 pips, Risk 20.0 pips, sweep "Low purged", expansion label "Expansion · exit at 90%". Uploaded a real generated PNG through the Charts card's "Add" link and confirmed via `psql`/Storage API it landed at `{user_id}/{tradeId}/{filename}` with its width/height captured, and that clicking the thumbnail opened it full-size in the lightbox. Opened Edit, confirmed every field — including notes, tags, and the existing attachment — was correctly pre-filled, added an exit reason and hold time, saved, and confirmed the view re-rendered with "Hold 38m", the exit reason, and a new "Edited HH:mm" footer stamp alongside the original "Logged" one, while a direct `psql` query confirmed `r_value_at_entry` was unchanged and `updated_at` had advanced. Deleted the trade through the confirm modal and verified via `psql` that the trade and its `attachments` row were both gone, and via the Storage API that the underlying object folder was genuinely empty, not just dereferenced. Test trade, account, and user deleted afterward.

## Phase 4c follow-up — three usability fixes reported after real use

The user hit all three while actually using the app on their own account (not synthetic testing), which is exactly the kind of bug real usage surfaces that a form built against one demo trade doesn't.

**1. Combobox always filtered by the current value.** `Combobox` computed its dropdown list by filtering `options` against the field's own current value on every render, so once the field already held a valid preset (e.g. "EURUSD"), opening the dropdown showed exactly one match — switching pairs meant deleting the text first. Fixed by splitting "the list is open" from "the user is actively filtering it": opening the field (focus, arrow key) now always shows the full unfiltered list with the current value's row scrolled into view and highlighted (`open()` in `combobox.tsx`, tracked via a new `isFiltering` state that starts `false` and only flips `true` from the input's own `onChange`, i.e. the user actually typing a character). The `.slice(0, 8)` cap on the list was also dropped — the dropdown already scrolls (`max-h-[264px] overflow-y-auto`), and the full preset list is only 28 items, so there's no reason to hide the rest of it.

**2. Clipboard paste for attachments.** TradingView's own chart-capture puts an image directly on the clipboard; round-tripping through Save-to-file → Choose file just to get it into a trade was the actual friction reported. Added `usePasteAttachment` (`src/lib/use-paste-attachment.ts`), a small hook that listens for a `paste` event at `window` — not scoped to the dropzone having focus, since after alt-tabbing back from another app whatever last had focus on the page is unpredictable — and only acts when the clipboard actually contains an image (`item.type.startsWith("image/")`), so it never interferes with an ordinary text paste into some other field on the same page. Wired into all three places a screenshot can be added: the New trade form, Trade detail's edit form, and Trade detail's own Charts-card "Add" (view mode). Each Dropzone's hint copy now mentions "paste with ⌘V"; the Charts card additionally keeps a small persistent caption once it already has attachments and its Dropzone (empty-state only) has disappeared, so the capability stays discoverable either way.

**3. A price wildly outside an instrument's normal range now warns.** The user pasted an index-style price (23,411) into a EURUSD field and got a mathematically-correct-but-meaningless "835000.0 pips" range size — the numbers were right, the input was wrong. Judged worth a warning (the user explicitly left this call open — "과하다고 보면 안 해도 된다") rather than skipping it, since it's the same class of mistake `result_disagrees_with_exit` already exists to catch, and it's cheap to check. Not a data table of 28 real historical per-pair ranges — a coarse "is this even the right order of magnitude" bound is all that's needed to catch a mistake four orders of magnitude off, without flagging anything merely unusual: `isPlausibleFxPrice` (`src/lib/instruments.ts`) uses `[0.2, 5]` for standard pairs and `[20, 500]` for JPY pairs (JPY sits ~2 orders of magnitude higher, same split as the existing decimal-precision rule), comfortably wide of every real modern-era major/cross. A new `price_implausible_for_instrument` warning in `collectWarnings` fires if any of rangeHigh/rangeLow/entry/stop/target/exit falls outside that band for the trade's instrument — non-blocking, like every other warning in this app.

**`collectWarnings`'s `WarningInput` gained an `instrument` field** (both the New trade form and Trade detail's edit form already had it in scope, so this was a one-line call-site change in each).

**Live verification hit a real obstacle, worked around without touching real data**: the browser session already had the user's own account signed in (no "Set up your account" gate, and — tellingly — the very draft that reproduced their reported bug, complete with the "835000.0 pips" figure). `document.cookie` is blocked from JS for this session and a direct `psql` read of `accounts` was blocked by the permission classifier, so there was no safe way to confirm identity except by what rendered; closing the Chrome window didn't clear the session either (Supabase's auth cookie is long-lived, not tied to browser-process lifetime). Rather than risk writing to the real account, verification split three ways:
- Combobox (fix 1) and the price warning (fix 3) were confirmed **read-only, against that same real draft** — opening the Instrument dropdown and scrolling to check the warning card never mutates anything, so this was safe and also happened to be a direct live confirmation of the exact reported bug.
- The scroll-to-selected-option behavior specifically (harder to see with EURUSD, which already sits first in the list) was confirmed separately on `/dev/components`, which is pure client-side demo state with no Supabase calls at all regardless of who's signed in — set the combobox to NZDCHF (near the bottom of the 28-item list), closed it, reopened it, and confirmed it jumped straight back to NZDCHF's row rather than the top.
- **Clipboard paste (fix 2) was not exercised live** — every avenue to get a clean disposable-user session was blocked or unavailable (cookies unreadable, DB read blocked, no logout route exists yet, and closing the browser window didn't end the session). It reuses the exact same `addFiles` path already end-to-end verified in Phase 4b/4c (upload → Storage → DB row → signed-URL preview); the only new code is the clipboard-to-`File[]` extraction in `usePasteAttachment`, which is straightforward enough to review by reading rather than requiring a live upload to trust. Flagged here rather than silently claimed as verified — worth a 10-second manual spot check (copy a screenshot, ⌘V on the New trade or Trade detail Charts card) whenever convenient.

The user confirmed paste itself works and reported the actual bug: thumbnails rendered at the pasted image's original aspect ratio, stacked vertically below the dropzone — a tall narrow sliver of a landscape TradingView capture, unrecognizable as a chart.

## Phase 4c follow-up — one shared attachment thumbnail component, replacing two different broken ones

There were, it turned out, two different thumbnail treatments already in place, and neither was right:

- **New trade form + Trade detail's edit form** used a `grid-cols-3` grid of `aspect-square` tiles. Square should have worked (`object-cover` crops to fill), but a landscape screenshot cropped to a square still throws away most of a chart's horizontal detail — recognizable, but not by much.
- **Trade detail's view-mode Charts card** followed `docs/README.md`'s own mock spec literally: 150px-tall, full-card-width slots stacked one per row (`flex-col`). That's a real spec, but the mock's placeholders are a striped pattern with a caption, not an actual image being cropped — the spec was never validated against a real screenshot's aspect ratio, and a real landscape capture stretched into a very wide, short strip doesn't read as a thumbnail so much as a banner.

Neither matches what the user actually reported seeing (tall and narrow), which suggests the real complaint was less about either specific existing layout and more that the two screens disagreed with each other and neither was designed around a real photo. Asked directly to unify them per the user's own fallback spec (fixed 16:9 or square, `object-fit: cover`, horizontal layout beside/inside the dropzone, hover-reveal remove, click-to-lightbox) rather than trying to reconcile two different existing designs.

**One new component, `src/components/attachment-thumbnails.tsx`, used identically by all three surfaces** (New trade form, Trade detail's edit form, Trade detail's own Charts card):

- **16:9 over square** — chosen over a square crop specifically because a chart screenshot is landscape; 16:9 keeps far more of the actual chart than a square crop would, while still being a fixed, predictable shape. Fixed `h-90 w-160` tiles, `object-cover`, `shrink-0` so they don't stretch in a flex row.
- **`flex flex-wrap`, not a scroll container** — reads as "grid" for a handful of screenshots (the realistic case: docs/README.md's own copy suggests two per trade) and simply wraps rather than requiring a horizontal-scroll gesture, which is worse ergonomics on a desktop form for such a small number of images.
- **Below the dropzone, not literally beside it** — beside wasn't practical without redesigning the layout `docs/README.md` §4 already locks in for New trade's Chart & notes step (an explicit 2-column dropzone-left/notes-right split, no third area). Kept directly under the dropzone in the same column, which was already where it lived; what changed is the shape, not the position. Flagged here in case the user wants literal side-by-side placement enough to justify revisiting that 2-column spec.
- **Owns its own lightbox internally** — before this, only Trade detail's view mode had one; New trade and the edit form had none. Centralizing it means every attachment surface gets click-to-enlarge for free instead of three copies of the same `Modal` wiring, one of which (Trade detail's) is now deleted.
- Trade detail's Charts card keeps its own surrounding chrome (the "Add" link, the empty-state `Dropzone`, the "paste to add another" caption once attachments exist) — only the thumbnail-rendering portion was replaced.

**Verification, again working around the same real-session problem** — this time by extending, rather than fighting, the `/dev/components` workaround from the previous entry: added a real `AttachmentThumbnails` demo there (`gallery.tsx`), wired to `usePasteAttachment` and to the existing Dropzone's `onFiles`, using `URL.createObjectURL` on real `File` objects rather than static demo data. Since `/dev/components` makes no Supabase calls at all, this is safe regardless of whose session is active, and it's a permanent, legitimate addition to the gallery besides (every other primitive already lives there). Generated a real two-color 320×180 PNG, set it on the actual OS clipboard via `osascript ... as «class PNGf»`, and confirmed by real ⌘V in the browser: the thumbnail renders as a fixed 160×90 tile with a clean centered crop (not stretched, not the original aspect ratio); hovering reveals the ✕; clicking it opens the lightbox at full original size (uncropped); clicking ✕ removes it; pasting a second image lays the two out side by side rather than stacking. Disposable test user created for this task was never actually needed (no Supabase-backed page was touched) and was deleted unused.

## Phase 4c follow-up — the thumbnail wasn't broken; a different app was

The user reported the exact same "tall, uncropped" symptom persisting on the real `/trades/new` after a hard reload *and* a full dev server restart, and reasonably suspected the fix was never actually wired into the real form despite matching in `/dev/components`. That distrust was earned — the previous entry's verification never touched the real page — so this was worth taking seriously rather than re-asserting the same evidence.

**What was actually checked, in order:**
1. Re-read `new-trade-form.tsx`'s attachment section end to end for a second, unwired render path (the user's own hypothesis: maybe the "uploading" placeholder renders differently from the settled state). There is only one path — `useDraftAttachments.addFiles` appends to state *after* the Storage upload resolves, and the only thing shown during upload is a plain "Uploading…" text line, no image. `AttachmentThumbnails` is the only place a thumbnail is ever rendered from this form.
2. Grepped the actual compiled bundle the running dev server was serving (`.next/static/chunks/app/trades/new/page.js`) for `AttachmentThumbnails` (present), `aspect-square` (absent), `w-160`/`h-90` (present) — confirmed the served code was correct, but this was inspecting a file on disk, not literally what the browser executed, so it wasn't fully conclusive on its own.
3. Did a clean `.next` wipe and full server restart to rule out any dev-mode build-cache staleness.
4. **Built the sign-out feature the user asked for** (see below) specifically so a disposable test user could be used directly on the real page instead of arguing from indirect evidence.
5. Signed out, logged in as a fresh disposable user, pasted a real clipboard image into the actual `/trades/new` page, and read the live DOM directly via `getComputedStyle` — not a screenshot, not a bundle grep:
   ```
   containerClassName: "group relative h-90 w-160 shrink-0 overflow-hidden rounded-14 bg-divider"
   containerWidth: "160px", containerHeight: "90px"
   imgObjectFit: "cover", imgComputedWidth: "160px", imgComputedHeight: "90px"
   ```
   Exactly the fix, exactly as designed. The component was never actually broken or unwired.

**So what was the user actually looking at?** `docker ps` shows a container named `trading-journal-frontend`, up for 44+ hours, mapped to `0.0.0.0:3000->80`. It responds on `http://localhost:3000` with `<title>Minimal</title>` — this is the pre-existing, unrelated Prisma/Express trading-journal project flagged all the way back in the Phase 0 decisions entry ("an existing, unrelated trading-journal app... `docs/build-prompt.md` §0 explicitly says not to reference or reuse"). It was never touched or removed, just not the app in scope — and it's apparently still running, on the one port (3000) that every `pnpm dev` in this project has been bumped off of all session long, for the same reason each time ("Port 3000 is in use..."). It is a *different trading journal app* with its own dropzone/thumbnail UI, so a stale layout there reads as entirely plausible as "our" bug at a glance, and no amount of restarting or clearing `min-journal`'s build cache would ever change what port 3000 serves. The user confirmed testing "localhost:3001" when asked directly, which the bundle check matched — the mismatch was likely an old tab left over from earlier in this same long session (the dev server's port bounced between 3000/3001/3002/3003 all day depending on what was already bound each time it restarted), not a deliberate choice.

**Sign-out, built as requested, unblocks this permanently.** Single-user apps don't strictly need one, and it wasn't scheduled until Phase 9's Settings screen, but three rounds of "which session/account am I actually looking at" made it worth building now rather than later:
- `src/app/actions.ts` — a `signOut` server action (`supabase.auth.signOut()` then `redirect("/login")`).
- `src/components/nav/sign-out-button.tsx` — a plain "Sign out" button in a `<form action={signOutAction}>`, no client state needed. Deliberately not a dropdown "account menu": with exactly one user and exactly one action available today, a menu would be an abstraction with nothing to organize.
- Mounted via `TopBar` on `src/app/page.tsx` (the dashboard placeholder) — `TopBar` itself needed no changes, since its existing `right` slot already exists for exactly this. Nav `items` are deliberately left empty: Dashboard/Trades/Calendar/Playbook/Capital aren't built yet (Phase 5+), and linking to routes that 404 would be worse than no nav at all. This is the first real mount of `TopBar` outside `/dev/components`.
- Not added to New trade's or Trade detail's own headers — reachable via their existing "close"/"back" controls back to `/`, and the ask was specifically for `TopBar`.

## Phase 4d — mobile quick-log wizard

Same route, no new URL: `NewTradeForm` now branches its own return on `locale` (already exactly the mobile/desktop signal per the Phase 3 viewport-locale decision — `ko` is `<900px` by construction, so no second breakpoint check was needed) rather than mounting a different component at a different path. All state — the react-hook-form instance, `derived`, the draft-autosave hook, the attachment hook, `usePasteAttachment` — is created once in `NewTradeForm` and simply rendered through either the existing desktop JSX or the new `MobileQuickLogWizard` (`src/app/trades/new/mobile-quicklog-wizard.tsx`), so draft autosave and clipboard paste are the *same* running hooks in both layouts, not a parallel reimplementation that could drift.

**Step breakdown.** The mock (1d-quicklog) shows exactly one full screen — step 2's sweep-side question — plus README's chrome spec (✕, "N / 4", progress bar, "이전"/"다음" pinned footer). Everything else about the other three steps had to be inferred. Chose the obvious mapping: one wizard step per existing desktop `SectionCard` (Context / Range & sweep / Execution / Chart & notes), in the same CRT order, since the mock's own step count (4) matches the desktop section count (4) exactly and the full required-field set (instrument, date, direction, session, htf pairing, range high/low, entry, stop, size) has to land somewhere regardless of device. Steps 1, 3, and 4 render their fields as a plain stacked mobile form (a heading + the same `Field`/`Input`/`Segmented`/`Select` primitives, single column); only step 2's sweep-side question reuses the mock's big-question treatment, since that's the one place a genuine mock exists to copy.

- **Sweep side is asked directly on mobile, not derived**, per README's own line: "sweep side inferred from which extreme was purged (**or explicitly chosen in the mobile wizard**)." This isn't optional stylistic choice — on mobile, Range & sweep (step 2) comes before Execution (step 3), so the stop price the desktop derivation needs doesn't exist yet when the sweep question is asked. The four option labels are the mock's own literal copy ("저점 퍼지 · 롱 방향" etc.) — already anticipated as a `Segmented showCheck` demo in Phase 3's gallery, though the actual mobile wizard needed its own full-width stacked-row component (`BigChoiceRow`) since the mock's rows are much larger touch targets than the gallery demo's horizontal segmented control.
- **The "· 롱 방향" / "· 숏 방향" suffixes are descriptive, not a second binding to Direction.** Both sweep options are shown unconditionally regardless of what was picked for HTF bias in step 1 — CRT theory just conventionally associates a low sweep with an eventual long and a high sweep with a short, and the copy says so as a memory aid. Selecting a sweep option only ever writes `sweepSideOverride`; it never touches `values.direction`. Contradicting Step 1's own answer two steps later would violate the CRT-order principle the whole wizard is built on (each step adds information, never silently overwrites an earlier answer).
- **`tradeToEditInput`'s existing "only carry the sweep side forward as an override if it disagrees with derivation" logic (Phase 4c) doesn't apply here** — on mobile there's nothing to derive from yet, so every mobile-wizard selection is written as an explicit `sweepSideOverride`, always.
- Field-level validation on "다음" calls `trigger()` scoped to just that step's required fields (`WIZARD_STEP_FIELDS`), not the whole form — so a trader can't get stuck by an error on a field three steps ahead they haven't reached yet, but also can't skip past a step's own required inputs.

**A real bug, not a test artifact: the shared Next/Submit button reused one DOM node across the step transition.** `{step < 4 ? <Button type="button" onClick={onNext}>Next</Button> : <Button type="submit">Log trade</Button>}` sits at the same position in the tree on every step, so React updates the existing `<button>` element in place rather than replacing it — including flipping its `type` attribute. Clicking "Next" on step 3 called `trigger()`, which resolved and called `setWizardStep(4)` quickly enough (a microtask-scheduled promise callback) that the button's `type` attribute flipped from `"button"` to `"submit"` *while the browser was still processing that same click* — and the browser's native activation behavior, which runs after JS event handlers finish, saw a submit button and submitted the form. The result was a trade silently created from whatever steps 1–3 held, skipping step 4 (attachments/notes/tags) entirely, with no error and no visual sign anything had gone wrong. Reproduced twice, with two different input methods (`form_input` and literal keystrokes), which ruled out a testing-tool artifact before looking at the component tree. Fixed with distinct `key`s (`"next"` / `"submit"`) on the two branches, forcing React to unmount/remount instead of mutating the node — confirmed fixed by repeating the exact same step-3-to-4 transition afterward and landing correctly on step 4.

**Verified live** with a disposable test user at an actual `<900px` viewport (`resize_window` is unreliable in this sandbox for shrinking a freshly-maximized window — confirmed only after several retries and a brand-new tab, landing at 800px, still safely under the 900px breakpoint): stepped through all 4 screens: Context → Range & sweep (range inputs, then the sweep question appearing only once the range is valid, with the mini `RangeDiagram` preview live-updating to the tapped option, matching selected-state styling exactly per README's spec) → Execution (R:R auto-calculating to "1 : 3.0R", Result auto-suggesting "Win" from a typed Exit, "Realized +1.5R") → Chart & notes. Pasted a real image via ⌘V (same `osascript`-to-clipboard technique as the earlier attachment-thumbnail verification) and got the same fixed 160×90 cropped tile as desktop. Force-quit the debounce window, reloaded the page cold, and confirmed the draft restored every field correctly, including the pasted attachment and its Korean notes text — the same `drafts` row and Storage path desktop uses, since it's the same hook. Submitted for real and confirmed via `psql` the trade landed with every wizard-collected field intact and the draft row deleted. Separately smoke-tested the desktop path (≥900px, a fresh disposable user) to confirm the shared `onResultChange` extraction didn't change desktop's own auto-suggest behavior. All test users, trades, and Storage objects deleted afterward.

**One incidental self-inflicted issue during this session, noted for the record, not a code bug**: running `pnpm build` (production mode) while a `pnpm dev` instance was live against the same `.next` directory corrupted the dev server's webpack module manifest (`Cannot find module './387.js'`), transiently 500ing every route until `rm -rf .next` + a clean `pnpm dev` restart. Don't run a production build against a `.next` directory an active dev server is using.

## Phase 4d follow-up — four issues from real use on an actual phone-width viewport

- **Step 1's Korean heading changed from "맥락" to "컨텍스트"** — a plain wording preference, not a translation error; both are correct Korean for "Context," the user just prefers the loanword here.
- **The four behavior tags (On plan / Impatient / Chased entry / Early exit) now read in English on both locales.** `TAG_PRESETS` in `src/lib/labels.ts` had real Korean translations (계획대로 / 조급함 / 추격 진입 / 조기 청산); the user types and reads these four specifically in English regardless of viewport, so `ko` was set equal to `en` for just this one array rather than leaving translations that would never actually be wanted. Every other label in the app keeps its real Korean translation — this is a one-off, user-stated exception, not a new i18n policy.
- **The sweep-side mini `RangeDiagram`'s "스윕" label was overlapping the subtitle line above it.** `RangeDiagram`'s sweep label is positioned *outside* its own box (`top:-34px` when the high side was swept, `bottom:-34px` for low), which reads fine on desktop only because the surrounding `Panel`'s padding happens to absorb it — the mobile wizard's tighter `gap-14` stack didn't leave room, and a high-side sweep's label landed directly on the HTF-pairing/range subtitle text above it. Fixed with `my-40` on the diagram itself (independent of the parent's `gap`, since the required clearance is asymmetric per sweep side and a single shared gap value can't express that). Verified specifically with a **high** sweep — the case that pushes the label upward into the subtitle — since that's the direction the user's own garbled example ("M → W 스윕2D 레인지...") implied.
- **Steps didn't scroll to top on transition**, since both the sticky header/footer and the step content live in the ordinary document flow (no dedicated scroll container), so advancing/going back kept whatever scroll offset a taller previous step had left behind — landing mid-step-3 showing only a stray "2" from the progress counter, with the "실행" heading and every field label scrolled out of view above the fold. Fixed with a `useEffect([step])` in `MobileQuickLogWizard` calling `window.scrollTo(0, 0)`.

**Verified live**, all four together, at an actual sub-900px viewport (a fresh disposable user, same `resize_window`-then-fresh-tab workaround as before — landed at 500px this time): confirmed "컨텍스트" on step 1; advanced to step 2, picked HTF pairing `M → W → 2D` (matching the user's own reported example) and a **high** sweep specifically, and confirmed "스윕" now sits cleanly above the diagram with no overlap with the "M → W → 2D 레인지 1.26050 — 1.26550" subtitle; advanced to step 3 and confirmed it opens scrolled fully to the top ("실행" heading visible, not the "2" cutoff the user described); left Entry/Stop/Size at placeholder (unfilled) and confirmed `trigger()`'s per-step validation correctly held the wizard on step 3 with inline errors rather than silently advancing; filled real values and confirmed step 4 also opens scrolled to top, with "On plan / Impatient / Chased entry / Early exit" now in English. Test user, draft, and account deleted afterward.

## Phase 4c follow-up — Trade detail was never checked below 900px

Trade detail (view mode `trade-view.tsx`, edit mode `trade-edit-form.tsx`, header `trade-detail.tsx`) was built and verified in 4c against the desktop mock only (`docs/README.md` mocks it at 1000px, no mobile mock exists at any width) — the same "designed, not mocked" situation Phase 3's `EmptyState`/`BottomTabBar` were already in, and the model followed here. Four issues, reported from a real phone screenshot, all traced to the same root cause: every multi-column `grid`/`flex` layout in both files was written once, for desktop, with no mobile branch at all.

- **Fix mechanism**: reused `locale === "ko"` — this app's existing mobile/desktop signal, which flips at exactly 900px by construction (Phase 3) — as `isMobile` in both `trade-view.tsx` and `trade-edit-form.tsx`, exactly as 4d already did for New trade. No second breakpoint mechanism was introduced for the structural changes: the Result card's `grid-cols-4` info tiles become `grid-cols-2` (a 2×2 layout, matching the user's own suggestion) at mobile width, the CRT-sequence `Panel`'s `grid-cols-[1fr_190px]` becomes a stacked `flex-col` with a top border replacing the left border, the bottom Charts/Notes `grid-cols-2` becomes `grid-cols-1`, and every field-pair row in the edit form (`grid-cols-2/3/4`) collapses to one column each — except Execution's Entry/Stop/Target/Size row, kept as a 2×2 `grid-cols-2` rather than fully stacked, since four numeric inputs read fine two-up and a full single column there would make the longest section in the form needlessly taller for no legibility gain.
- **The caption-overlap bug ("0.641900.648020.65414") was a `flex justify-between` bug, not a mobile-layout bug, and was fixed once at the shared component level.** `justify-between` doesn't guarantee a minimum gap — once three captions' natural width exceeds the container (routine for 5-decimal FX prices on a narrow card), the distribution collapses toward zero and the values visually concatenate. Fixed in `src/components/range-diagram.tsx` by switching the caption row from `flex justify-between` to `grid grid-cols-3 gap-8` with per-column text alignment (`text-left`/`text-center`/`text-right`), which guarantees the gap regardless of overflow and lets an individual caption wrap onto a second line instead of merging into its neighbor. Since New trade's desktop form, the mobile wizard, and both of Trade detail's modes all render captions through this one component, the fix applies to all four call sites without touching any of them.
- **Header title wrapping** (`trade-detail.tsx`): fixed with a plain, unprecedented Tailwind `sm:` (640px, Tailwind's default) breakpoint, deliberately *not* the app's own `locale`-driven 900px signal. Reasoning: the entire 0–899px span already renders Korean/mobile styling by the existing rule, but a phone at ~380px and a "mobile-styled" browser window at ~850px have very different amounts of header room — a secondary, narrower breakpoint purely to stop the title from wrapping is a legitimate refinement *within* the mobile zone, not a competing layout system. `formatTradeDate` gained a third `compact` parameter (default `false`) that omits the year — below `sm`, the header shows the compact date (e.g. "9월 11일"); at or above it, the full date with year. `min-w-0 truncate` on the `<h1>` remains the backstop for a long free-typed instrument name at any width.
- **Self-caught mistake, fixed before any verification**: first pass at the Result card's mobile Realized-R size used `text-36`, which silently produces no CSS at all — this project's type scale is a closed set of custom tokens (`FONT_SIZES` in `src/lib/cn.ts`), not Tailwind's dynamic scale, and 36 isn't one of them. Caught by checking against `FONT_SIZES` before running anything; corrected to the existing `text-30` token.
- **Other Phase 4 screens checked per the user's request**: New trade's desktop JSX (`new-trade-form.tsx`, structurally identical grids to the edit form fixed here) is entirely inside `if (locale === "ko") { return <MobileQuickLogWizard ... /> }` — confirmed by reading the branch — so it's unreachable below 900px and needed no changes. `AccountSetup`'s modal (shown when a fresh install opens New trade with no account yet) is already single-column with no grids, and `Modal`'s own non-sheet variant is `w-full max-w-[520px]` inside a `p-16` backdrop, i.e. fluid rather than a fixed width, so it and the Delete-confirmation modal in `trade-detail.tsx` (same `Modal` component) both already shrink correctly on a narrow viewport with no changes needed.

**Verified live** with a disposable test user and a trade matching the user's reported scenario as closely as possible (AUDUSD short, 5-decimal range/entry/stop/exit prices, a `holdMinutes` value, notes, and two tags) at an actual sub-900px viewport (~605px CSS width this session): header renders as a single line ("AUDUSD · 숏 · 9월 11일") aligned with the back button; the Result card's four tiles render as a clean 2×2 grid with no overflow and "보유 시간" no longer splitting vertically; the range diagram caption row reads "저점 0.64190 · 50% 0.64802 · 고점 0.65414" with clear spacing, no concatenation; the Charts and Notes cards stack vertically at full width. Repeated the same checks in edit mode (`trade-edit-form.tsx`): all field-pair rows single-column, the Entry/Stop/Target/Size row a clean 2×2, the range/sweep `Panel` stacked with the diagram on top and stats below. `npx tsc --noEmit`, `pnpm lint`, `pnpm test` (140 passed), and `pnpm build` all clean. Test user, account, and trade deleted afterward (no Storage objects were created in this pass, so no bucket cleanup was needed).

## Phase 5 — Dashboard + Calendar

Both screens leaned entirely on selectors Phase 2 already built and tested (`periodStats`, `equityCurve`, `byModel`, `bySession`, `dailyNetR`) — the only new domain code is `src/lib/domain/dates.ts` (month math: `monthRange`, `shiftMonth`, `buildMonthGrid`, `formatMonthLabel`, all plain string/UTC-date arithmetic, no date library, consistent with Phase 2's "dates are strings" rule) and one new selector, `sweepAlignment` in `stats.ts`.

**Dashboard's scope is the current calendar month, full stop — hero, stat row, both analysis cards, and Recent trades all read from the same month-scoped trade list.** The hero's own label ("Month to date") and the header's month display already imply this; extending it to every other card keeps "61 trades" (hero chip), "View all 61" (Recent trades), and Win streak's `current` all describing the same period rather than three different implicit scopes. **Win streak specifically is therefore the current run *within the displayed month*, not lifetime** — a judgment call, since docs/decisions.md's Phase 2 entry only settled *that* the dashboard shows `current` over `longest`, not what period it runs over. The alternative (lifetime current streak) would need trades from prior months just to render one stat on an otherwise month-scoped screen, which reads as more surprising than a streak that resets with the calendar.

**`sweepAlignment` (new selector) is not the same grouping as `bySweepSide`.** The "Session · sweep side" card's three win-rate bars — "Long after low purge" / "Short after high purge" / "Entry without a sweep" — cross sweep side with direction (a long entry after a *high* purge isn't the textbook CRT setup even though `bySweepSide` would lump it into the same "high" bucket as a short). Implemented as three fixed, named filters rather than a generic cross-tab, since these three specific combinations are the only ones the mock names.

**Off-plan model rows get their note from existing `GroupStats` fields, not a new flag**: `docs/README.md`'s "· off-plan entries" caption appears when `offPlanCount === tradeCount` for that model group (every one of its trades is off-plan) — cross-checked against the mock's own example (the retired "Mid-range entry" model, whose trades are all `sweepSide: 'none'` per the Phase 2 writeup).

**Mobile home's "잘 되는 모델" (Models at work) shows a curated 3, not the desktop list's first 3.** The mock's own sample data picks the top two models by netR plus the single worst one, skipping over a third-place model in between — reproduced exactly as `topAndBottomModels`: sort by netR descending, take `[0, 1, last]` when there are more than three groups. This is a different ordering rule from the desktop card, which keeps `byModel`'s own Playbook-`sortOrder`-then-Unassigned order unchanged. Both are legitimate: desktop has room to show every model that traded, so it reads in the trader's own configured order; mobile has room for exactly three, so it shows the two standouts and the one to watch.

**Heat scale maps onto existing color tokens with no new CSS**: the five buckets in docs/README.md's table are pixel-identical to this app's own `gain-tint-1/2/3` / `gain-deep-1/2` / `loss` / `loss-tint` tokens (already defined in Phase 0 for the KR P&L convention), so `src/app/calendar/heat-scale.ts` is a plain bucket function returning existing Tailwind classes, not new hex values. A day that trades to exactly breakeven (`netR === 0`) is deliberately styled identically to a no-trade day — the table's own "0 / no trades" row groups them, and there's nothing meaningful to color or caption for either.

**Calendar's mobile grid shows no day numbers at all, matching the mock exactly** — every cell in mock 1d's calendar (populated or blank) omits the day number, relying on the tap-to-open day-detail card below for date identification rather than a number in the cell. Confirmed this is consistent across every cell in the canvas markup (not an isolated omission), so it's read as a deliberate minimalist choice for a 42px cell rather than a mock oversight worth correcting.

**Both screens mount the real `TopBar`/`BottomTabBar` for the first time outside `/dev/components` and the placeholder home page**, via a new shared `src/components/nav/routes.ts` (`NAV_ROUTES`, each with an `enabled` flag) instead of two hand-maintained lists. `enabled: false` for Trades/Playbook/Capital (not built yet) — **`TopBar`/`BottomTabBar` gained a `disabled` per-item flag** that renders a plain, non-interactive `<span>` at 40% opacity instead of a `Link`, so an unbuilt destination never becomes a dead link into a 404. The `/dev/components` gallery's own nav demo was switched to import `NAV_ROUTES` too (dropping its independent copy) — it now shows the *real* current enabled/disabled state rather than a decorative "everything active" mockup, which is arguably more useful for a dev-only page anyway.

**Calendar's own header (month stepper + Week/Month toggle) sits below the shared `TopBar`, not instead of it** — mock 1c omits the app-wide top bar entirely, but every other top-level screen (1a dashboard, 2a trade log, 3a capital) draws it, and README's own Interactions section lists Calendar as a `Nav` destination like the rest. Read as an economy-of-effort omission in the mock (identical markup repeated three times already), not a deliberate "Calendar has no nav" design. `TopBar`'s right slot on Calendar is just "New trade" (matching 2a Trade log's pattern) — no month label, since that's Calendar's own stepper's job now.

**"Week" is rendered but not wired.** The mock's Week/Month toggle is drawn, but nothing anywhere in docs/README.md specifies what a week view actually shows — no mock, no data shape, no interaction spec beyond the toggle's own existence. Built the control with "Month" active and functional and "Week" visually present but inert (`aria-disabled`, no handler), rather than either inventing a week-view design or leaving the toggle out of fidelity. Flagged here rather than silently shipped as if it worked.

**Desktop day-hover tooltip is CSS-only** (`group`/`group-hover:opacity-100`), not a JS-positioned tooltip library — matches this app's existing "keep motion light" bar for hover states and needed no new state. Click-to-view-a-day reuses the existing `Modal` primitive on desktop; mobile has no modal in the mock at all, so it's an inline card below the grid instead (mirroring mock 1d's own always-visible day-detail card), with the current day pre-selected as a sensible default only when the displayed month is genuinely the current month.

**A real bug, caught by testing month navigation rather than a single render**: `MobileCalendar`'s `selectedDate` state defaults to "today" via a `useState` initializer, which — because `/calendar?month=...` navigation stays on the same route and Next.js's App Router therefore reuses the existing component instance rather than remounting it — kept pointing at the *previous* month's date after pressing `‹`/`›`, showing a stale "September 10" day-detail card while the grid itself had already updated to October. Fixed by keying both `DesktopCalendar` and `MobileCalendar` on `month` at the one call site (`CalendarView`), forcing a clean remount on every month change instead of hand-syncing each piece of local state — the same "force a remount instead of patching state" instinct as the Phase 4d button-type bug fix, applied preemptively to `DesktopCalendar` too even though its own default state (`null`) happened not to go stale.

**Trade-count copy needed a real singular case** (`"1 trades"` rendered live before the fix) — every trade-count caption across both screens (hero chip, model rows, session tiles, calendar tooltips, the day-detail heading) now goes through one new `tradeCountLabel(n)` helper in `src/lib/labels.ts` rather than five-plus inline `${n} trades` template pairs, so this doesn't have to be re-caught per call site. Caught live, not in code review — the math was already correct (a model with exactly one trade this period), only the English pluralization was wrong.

**One incidental fix to `formatR`**: gained a `forceSign` parameter (default `true`, preserving every existing call site). The mock is inconsistent on its own terms — the hero headline, Recent trades' result column, and the model/session R breakdowns all carry an explicit "+" on a positive value, but the Avg win stat and the hero's expectancy chip render positive values with no sign at all ("2.3R", "0.30R"). Matched both readings exactly rather than picking one convention and overriding the mock.

**Empty states, per the user's request**: both screens degrade gracefully with no special-cased "if empty" branch beyond the Dashboard — a Calendar with zero trades in the viewed month already reads correctly as an empty state (an all-`bg-surface-faint` grid, "0" trading/green days, em-dashes for best/worst), so no dedicated empty-state UI was built there. The Dashboard replaces its entire hero/stats/analysis/recent-trades stack with one `EmptyState` card when the month has zero trades, with copy that distinguishes a genuinely account-less fresh install ("Log your first trade") from an existing account whose current month just hasn't started yet ("No trades logged this month yet" — deliberately not "no trades ever", since that would misread for an active trader on day one of a new month). Both variants' CTA goes through the same `/trades/new` route, which already owns the account-setup gate.

**Verified live**, both screens, both viewports, against a disposable test user seeded with ten trades spanning Sept 2–10 2026 across three models (one retired, all-off-plan), all three sessions, both directions, an open trade, and a breakeven trade — deliberately shaped to exercise every branch (multiple heat buckets, the off-plan note, the no-sweep bar, an empty vs. a real day, a stale-month-nav regression). Checked every displayed number against the seed data by hand before trusting the screen: hero "+9.4R"/"10 trades"/"56% win rate"/"1.04R expectancy", the equity curve's "Max drawdown −2.0R" caption, Avg win "2.4R"/Avg loss "−0.9R", Win streak "3 trades", Rule adherence "80%", all four Performance-by-model rows (including the off-plan note on the one all-off-plan model), all three session tiles, and all three sweep-alignment percentages (75%/67%/0%) — every one matched a hand-computed value from the raw trade list. Confirmed a Recent trades row's click-through lands on the correct real Trade detail page. Repeated the equivalent checks on Calendar: per-day hover tooltip contents, click-to-modal's trade list and total, the summary row's four values, and the ArrowLeft/ArrowRight + `‹`/`›` month navigation (including the mobile stale-selection bug above, caught specifically by navigating months rather than only checking one render). Checked both screens' account-less and zero-trades-this-month empty states with a second, fully fresh disposable user. Confirmed disabled nav items (`Trades`, `Playbook`, `Capital`) render inert and never navigate on click, on both `TopBar` and `BottomTabBar`. `npx tsc --noEmit`, `pnpm lint`, `pnpm test` (154 passed), and `pnpm build` all clean throughout. Both test users, their accounts, and all seeded trades deleted afterward (no Storage objects were created in this pass).

## Calendar widened past its 900px mock

The user's own read: at a wide monitor the 900px-capped card (matching the mock's authored width, same treatment Trade Detail got at 1000px) left most of the screen as bare page background, and a calendar specifically gets *more* legible with bigger day cells rather than less. Raised `DesktopCalendar`'s content `max-w` from `900px` to `1600px` — deliberately wider than Dashboard's own 1200px cap, since a 7-column grid has more to gain from extra horizontal room than a card-based layout does, and Dashboard was explicitly told to stay put. Nothing else about the grid changed: day cells are still `h-78`, only their width grows with the wider columns; the legend, summary row, and header bar are unaffected. `MobileCalendar` (the `<900px` component) was untouched — it already keeps 7 columns at a fixed `h-42` regardless of any desktop change, since the two are separate components on the locale branch, not one shared responsive layout.

## Phase 6 — Trade log

Filtering/sorting/pagination is one new pure module, `src/lib/domain/trade-log.ts`, over the account's full trade list — same architecture as `stats.ts`'s selectors, so the table, the summary row, and CSV export all derive from the exact same functions rather than three slightly-different implementations. The route is fully server-driven: `src/app/trades/page.tsx` parses `?from=&to=&instrument=&session=&model=&sweep=&result=&offPlan=&sort=&dir=&page=` from the URL, fetches every trade for the account once (`getAllTrades`, no date-scoping — filtering is a client concern now, not a query concern), and does the filter → sort → paginate pipeline server-side before handing the client component only the current page's rows plus the pre-computed summary. Every filter pill, sort-header click, and pager button is a plain `router.push` to a new `?`-string built by `buildTradeLogSearchParams` — no client-side re-filtering of an already-fetched list, so the URL is genuinely the only state, matching README's "filter state lives in the URL" requirement exactly rather than approximately.

- **Page size is 20, not the mock's illustrative 5** ("Showing 5 of 18" in the canvas is clearly a small sample screenshot, not a spec) — chosen as a plain, ordinary table page size; nothing in README ties a number to it.
- **`sweepAlignment`'s cousin problem, solved the same way New trade already solved the model-name-vs-relative-magnitude question**: this screen's Session/Model/Sweep/Result filters are genuinely independent single-or-multi-select dimensions, so one new shared component, `FilterDropdown` (`src/app/trades/filter-dropdown.tsx`), covers all four rather than four bespoke controls — a `multiple` prop is the only thing that differs between "Instrument/Session/Model" (checkbox list, closes only on blur) and "Sweep side/Result" (single pick, closes immediately on selection). Closes via the same blur-timeout convention `Combobox` already established in Phase 1/3, not a new document-click-outside listener.
- **A fresh sort click defaults direction per column, not globally**: text-shaped columns (Instrument/Model/Session/Sweep) start ascending, date/Planned/Result start descending — a plain client-side judgment call (not URL-encoded as a "default"; `buildTradeLogSearchParams` still omits `dir` only when it equals the one global default, `desc`), matching the ordinary expectation that clicking "INSTRUMENT" for the first time shows A→Z, not Z→A.
- **Model sort/filter treats `null` (no model) as "Unassigned"** the same way Dashboard's Performance-by-model card already does, sorting as an empty string (first, ascending) — consistent rather than inventing a second convention for the same concept.
- **CSV export is a Route Handler (`src/app/trades/export/route.ts`), not a client-side blob download.** It re-derives the filtered set from the *same* query string the page itself reads (`parseTradeLogFilters`/`parseTradeLogSort` again, no pagination applied) rather than shipping the client an unpaginated trade list just so a button has something to serialize — the "Export CSV" control is a plain `<a href="/trades/export?...">`, and the browser's own download handling does the rest. Columns mirror Import's own field list exactly (enum-like columns write the English label, e.g. "Low purged", not the raw code) plus two derived, non-importable columns (Planned R, Realized R) appended for a human reading the file — Import's own mapping step ignores any column it has no field for, so the extra columns don't confuse a straight export-then-reimport round trip.
- **CSV parsing is 60 lines of hand-written RFC 4180, not a dependency** (`src/lib/csv.ts`) — quoted fields, doubled-quote escaping, embedded commas/newlines, all unit-tested directly. In line with this codebase's running preference (`clsx`/`tailwind-merge` were the one prior exception, added only because merging conflicting Tailwind classes correctly isn't reasonably hand-rollable) for writing a small, correct, testable piece over a new dependency when the grammar is this contained.
- **Import's column-mapping step is required precisely for `CSV_REQUIRED_FIELDS`** (the same NOT-NULL set Phase 4a already established for the New trade form, plus `rValueAtEntry`) and optional for everything else; a best-effort `guessMapping` pre-fills any column whose header matches a target field's key or either locale's label, but never blocks on a miss — the user confirms or fixes every mapping before the mapped rows are ever parsed.
- **`rValueAtEntry` is read directly off the CSV row, never recomputed** — the one deliberate divergence from `createTrade`'s own behavior, and exactly what docs/decisions.md flagged back in Phase 4a: "Phase 6's CSV import will need a per-row value, since historical trades' 1R can't be derived from today's balance." A row missing it, or with a non-positive value, is rejected in the preview step like any other invalid row.
- **A model name that doesn't match any existing model is treated as Unassigned, not a rejection** — a typo'd or since-renamed Playbook entry in someone's historical CSV shouldn't sink an otherwise-valid row over one non-critical field.
- **Enum-like columns (Direction/Session/HTF pairing/Sweep side/Result) accept either the raw stored code or either locale's display label**, via the same `resolveLabel` helper Trade log's own filter dropdowns use nowhere near as forgivingly — reused here specifically because a hand-authored or spreadsheet-exported CSV is far more likely to contain "Long"/"롱" than the internal code `long`.
- **A real bug, found by actually importing a CSV rather than only unit-testing the schema in isolation**: every row using an HTF pairing written as "W -> 2D" (a plain ASCII arrow, which is what any spreadsheet or hand-typed CSV will actually contain) was rejected as "Unrecognized HTF pairing", because `resolveLabel` did a literal string compare against `HTF_PAIRING_LABELS`' stored "W → 2D" (the real "→" glyph). Fixed by normalizing both sides of the comparison — collapsing `->` to `→` and squashing surrounding whitespace — inside `resolveLabel` itself (`src/lib/labels.ts`) rather than only in the CSV path, since the normalization is inert for every other label set (none of them contain an arrow) and this way Direction/Session/Sweep/Result/HTF pairing all benefit from the same leniency by construction instead of three of the five getting it and HTF pairing needing a special case. Caught live: a hand-built test CSV with three realistic rows and one deliberately-broken row initially came back "0 ready to import, 4 rejected" — all three good rows failing on the HTF pairing column specifically, not the deliberately-bad row, which was the tell that something systematic (not row-specific) was wrong.
- **Trade-count copy needed the same singular fix Phase 5 already made once**: the summary row's "Matching" stat now goes through the existing `tradeCountLabel` helper instead of a fresh inline `${n} trades` string, so this doesn't need a third catch later.
- **Dashboard and mobile home's "View all N" / "전체보기" links, previously inert placeholders** (Phase 5, before Trade log existed to link to), now route to `/trades` pre-filtered to the same period each card already summarizes — the desktop card's current calendar month (`from`/`to` from `monthRange`), the mobile "오늘" card's exact day (`from = to = today`) — reusing `buildTradeLogSearchParams` rather than hand-building a query string a second way.
- **No dedicated mobile mock exists for this screen** (`docs/README.md`'s own open-questions list: "mobile versions of screens 5–8" are undesigned) — but unlike Trade Detail, README's general responsive-rules table already specifies exactly what a mobile trade row should look like ("trade rows become the mobile two-line list: instrument + sub-line left, R right"), so this wasn't a blind design-from-vocabulary call: `TradeLogView` is one component, not a locale-branched pair, with `isMobile` collapsing the table into that exact two-line list and stacking the filter/export/import controls, following the same single-component-with-conditional-classes shape Trade Detail's own mobile fix used.
- **A real gotcha discovered mid-session, unrelated to any of this phase's own code**: a stray `next dev` process from earlier in this session had survived an intended `pkill` and was still bound to port 3000, so starting a second dev server for this phase landed on port 3001 — and the two processes sharing the same `.next` directory manifested as the `/trades/export` route hanging mid-compile indefinitely (a genuine multi-minute hang, not a slow first compile). This is the same class of hazard as the documented "don't run `pnpm build` against a `.next` directory an active `pnpm dev` is using," generalized to "don't run two `pnpm dev` instances against the same project directory at all" — fixed by killing both stray processes, wiping `.next`, and restarting exactly one.
- **A second real bug, unrelated to Trade log's own code, found only because testing crossed from a disposable session into a real one**: navigating to `/login` while an old, still-valid session cookie was live redirected straight into the signed-in account rather than showing the login form (middleware's own "already authenticated → bounce off /login" rule, working as designed) — and that session turned out to be the user's own real account (`nmxnues@gmail.com`), not a leftover disposable one, identified by cross-checking trade counts per user directly against the database before touching anything. No data was read or written beyond what the dashboard/calendar pages already render on load, and signing out (a read-only auth action) was the only interaction taken before switching to the intended disposable user. Also surfaced three genuinely orphaned disposable users from an earlier session's failed cleanup attempt (accounts that were created successfully but never matched by that session's own after-the-fact "did this ID exist" checks) — deleted alongside this phase's own test user.

**Verified live**, both viewports, against a disposable test user seeded with 35 trades spanning three months, four instruments, three sessions, two directions, an open trade, and a mix of models/off-plan trades — enough volume to force real pagination (20/page → exactly 2 pages). Confirmed: an instrument filter narrows the table and updates the URL/summary/pill state instantly; Reset clears back to a bare `/trades`; clicking a sort header sorts (checked Instrument ascending — alphabetical, correct default direction on a fresh click — and confirmed page 2 continues the same order rather than re-sorting per page); the pager's "Showing 1–20 of 35" and page buttons navigate correctly while preserving the active sort; the sweep-side single-select closes immediately on pick and updates the accent chip/URL; an impossible date-range filter renders the "No trades match these filters" empty state with a working "Reset filters" button; a Recent-trades-style row click lands on the real Trade detail page. Export CSV was checked via a same-origin `fetch` in the live page (not a literal browser download, which this sandbox's automation can't inspect) — confirmed the header row, per-row field values, and a filtered subset (`?instrument=EURUSD`, 9 of 35 rows) all matched the underlying data exactly, including `Planned R`/`Realized R` computed correctly. Import was run end to end with a real 4-row CSV using deliberately non-matching header names (to force the manual-mapping path rather than the auto-guess path) and one deliberately invalid row: after the HTF-pairing bug above was fixed, the preview correctly showed "3 ready to import, 1 rejected" with the right per-row reasons, and confirming the import landed exactly 3 new trades — verified directly via `psql`-equivalent queries that `htf_pairing`, `sweep_side`, `model_id` (resolved from free-text model name to the real UUID), `r_value_at_entry` (the literal CSV value, not recomputed), `notes`, and `hold_minutes` all landed correctly, and that the dashboard's Net R figure moved by exactly the two importable rows' combined realized R. `npx tsc --noEmit`, `pnpm lint`, `pnpm test` (196 passed), and `pnpm build` all clean. Test user, account, and all trades (seeded and imported) deleted afterward, along with three unrelated orphaned test users discovered from an earlier session.

## Trade log follow-up — the date-range pill leaked the browser's own locale, not this app's

The user's own report: on the desktop (≥900px, this app's own "always English" rule), the date-range filter's empty-state text read "연도. 월. 일." — Korean — because a plain `<input type="date">`'s placeholder (and, once filled, its own rendered value) is drawn from the *browser's* configured display language, not this app's `lang`/`t()`/`useLocale()` at all. Confirmed by testing: setting `lang="en"` directly on the `<input>` (the first attempted fix) had no effect — Chromium's date-input locale is not element-`lang`-driven, and there is no supported way to override it short of not showing that text to the user at all.

**Chose to move closer to the mock, not just patch the bug.** Re-reading the mock's own markup for this control shows it was never two visible date fields to begin with — `docs/README.md` § Trade log describes "a date-range pill (ink #191f28 when set)" and the canvas renders it as *one* summary string ("Sep 1 — Sep 9") plus a "▾", the same shape as every other filter dropdown on the row. The previous implementation (two native inputs inline, separated by an em dash) was already a step away from that shape before the locale bug made it worse. Fixed both at once: the closed pill now renders the mock's own summary text ourselves — `formatTradeDate` in the locale this component is always in (English, since it only reaches the ≥900px branch) — "All dates" / "From Sep 3" / "Sep 3 – Sep 5", literally never touching a native input's own text. Clicking it opens a small popover (closes on blur, same convention as `Combobox`/`FilterDropdown`) holding the two real `<input type="date">` elements, each under its own `t()`-driven "From"/"To" label — the *only* place a native date input's own browser-locale text can still show through, and only for the moment the user is actively picking a date, not as something permanently visible. `<900px` (`MobileCalendar`'s sibling, `DateRangePill`'s own mobile branch) was left exactly as it was: two inline native inputs with no popover, since that screen is Korean-only by construction and a Korean-language browser's own native picker text already matches what a Korean-reading user expects there — the mismatch was specifically an English-screen-showing-Korean problem, not a "native inputs are wrong" problem.

**Verified live**: with a Korean-configured browser, the closed pill on the desktop Trade log now reads "All dates ▾" (previously "연도. 월. 일. — 연도. 월. 일."); picking a from-date in the popover updates the pill to "From Sep 3 ▾" and the URL to `?from=2026-09-03` immediately; setting both ends renders "Sep 3 – Sep 5 ▾", matching the mock's own format exactly. Confirmed the `<900px` view is byte-for-byte unchanged — still the plain two-native-input row, in Korean, as before. `npx tsc --noEmit`, `pnpm lint`, `pnpm test` (196 passed), and `pnpm build` all clean. Test user and account deleted afterward.

## Phase 7 — Playbook + Weekly review

**Weekly review's schema already existed, unused, since Phase 0** — `weekly_reviews` (iso_week, what_worked, what_didnt, one_change, focus_items jsonb) was created in the very first migration but nothing had read or written it until now. It already matches the mock's real shape exactly (three separate text columns, not the single "text" field README's own State Management section sketches) — no migration needed this phase.

**Weeks are ISO 8601** (`YYYY-Www`, Monday-start, week 1 = the week containing the year's first Thursday), hand-rolled in `src/lib/domain/weekly-review.ts` since JS has no built-in ISO week support. Verified against the mock's own numbers before trusting it: Sep 9 2026 → `2026-W37`, and that week's range → Sep 7–13, both exact matches. Some ISO years have 53 weeks and some have 52 (2025 has 52) — worth noting since it's an easy thing to assume incorrectly when writing the tests for `shiftIsoWeek`.

**Playbook**

- **One accordion, `expandedId` state, defaulting to the first model** — matches the mock's own framing ("First card is the expanded model") rather than defaulting to nothing expanded.
- **Every field is inline-editable with no separate edit mode**: name and description are plain `<input>`/`<textarea>` sitting directly in the display layout (transparent background until hovered/focused, then the field affordance appears), saving on blur. Rules are the same pattern per line, plus add/remove buttons. This matches "rule lists are editable in place" literally rather than building a pencil-icon edit toggle the mock never shows.
- **A new model starts as literally "New model", empty, active, appended after every existing one** (`sortOrder = max + 1) — there's no separate "create" dialog to keep in sync with the inline-editing surface; renaming and adding rules immediately after creation is the same interaction as editing any other model.
- **Retiring/reactivating is immediate, no confirmation modal** — unlike deleting a trade (Phase 4c), this never destroys data; it's a reversible status flag with an immediate "Reactivate" escape hatch right next to it, so the safety bar that applies to irreversible actions doesn't apply here.
- **The one-line retired notice is systematic, not the model's own free-text description.** The mock's own retired example (`"Mid-range entry"`) happens to have a description that already explains the off-plan consequence, but that's the *user's* free text, not something to rely on — a model retired without rewriting its description would otherwise show no such explanation at all. Added a fixed, always-shown caption for `status === 'retired'` (both the collapsed row and the expanded card) instead, independent of whatever the description field says: "existing trades keep their record as logged; only new trades matching this model are flagged off-plan."
- **Retiring doesn't touch a single trade row, by construction, not by care** — `offPlan` is recomputed fresh from each trade's own stored fields on every read (docs/decisions.md § Phase 2), so there is no code path that could retroactively flip a historical trade's status even if it tried to. The action's own doc comment says so explicitly, since this is exactly the kind of invariant worth being loud about at the one place someone might think to "fix" it later.
- **No reordering UI.** The mock draws no drag handle or up/down control anywhere, and "the dashboard's model breakdown reads its order from here" only requires that *an* order exists and Dashboard respects it (already true — `byModel` sorts by `sortOrder`) — not that Playbook itself expose a way to change it after creation. Flagged here rather than silently scoped out.
- Two mock values fall outside this project's own closed design-token scales: the rule-number/checkbox badge's "radius-6" (not in README's own radius list, but used identically in two independent mock sections — Playbook's rule numbers and Weekly review's focus checkboxes — real repeated intent, so added as a genuine new `--radius-6` token rather than rounding up) and its "10px" numeral text (a single, one-off occurrence with no repeat elsewhere, rounded up to this project's existing `text-11`, the smallest step in `FONT_SIZES`, per the same reasoning Phase 3 already used for the invented 28px stat-card size). Caught both by cross-checking every new utility class against `src/lib/cn.ts`'s `FONT_SIZES` array and the theme's `--radius-*` tokens before running anything — the same self-check discipline `text-36` and `rounded-4` were caught with in earlier phases, not a build-time guarantee (Tailwind silently emits no CSS for either kind of miss).

**Weekly review**

- **"Vs. 4-week avg" is the mean net R per calendar week over the 4 ISO weeks immediately before the one being viewed**, treating a week with zero realized trades as 0 rather than excluding it from the average — an average "per week you actually traded" would flatter a habit of skipping weeks. The mock's own "+2.1R" / "avg +5.1R" pairing is read as (this week's net) − (prior average) for the headline figure, with the average itself as the sub-line.
- **Tag frequency's bar share is count ÷ that week's total trade count, not count ÷ the top tag's count** — reverse-engineered from the mock's own numbers: a week of 9 trades with tag counts 8/4/2/1 renders bar widths 89%/44%/22%/11%, which only lines up against 9 (the trade count), not 8 (the top tag) where the top bar would need to read 100%. The "weak" (grey) vs. full-ink threshold reproduces the same example exactly at "count < half of the top tag's count."
- **Focus items carry over by dropping the checked ones and keeping the rest, computed once at first view of a new week, not on every render.** `getOrCreateWeeklyReview` (`src/app/weekly-review/get-or-create.ts`) checks for an existing row first; only if the week has never been opened does it look at the previous week's row and seed `focus_items` with `carryOverFocusItems(previous.focusItems)` on insert. Opening the same week again later just reads the row as it now stands (including anything since checked off or added) — carry-over is a one-time seeding event per week, not a live merge that would fight the user's own edits.
- **"Copy last week's notes" needed no extra fetch** — the previous week's review was already being loaded to compute carry-over, so `getOrCreateWeeklyReview` returns it alongside the current week's row and the button just copies its three text fields into local (unsaved) textarea state, letting the user edit before committing with "Save review."
- **The three review textareas save only on the explicit "Save review" button; the checklist and its additions/removals save immediately on each interaction.** Matches the mock's own two different affordances for these — a persistent checklist that's always "live" versus a page-length editorial pass a trader closes out deliberately — rather than picking one autosave convention for both.
- **Not one of the five `NAV_ROUTES` destinations** — the mock's own turn-list frames it as reached from the Dashboard, not as a persistent nav tab, and README's Nav section only ever lists "Dashboard / Trades / Calendar / Playbook" as routes. Reachable via a plain "Weekly review" link added to the Dashboard's own header (both desktop's `TopBar` right slot and mobile home's top area) instead of inventing a sixth primary destination. `TopBar`/`BottomTabBar` render with no item marked active on this page (`activeHref=""`, matching nothing) rather than falsely highlighting Dashboard.

**Verified live** with a disposable test user seeded with 3 models (two active, one retired — matching the mock's own trio), 5 trades in the current week (2026-W37, Sep 7–13) across varied sessions/directions/tags/results, 4 trades in the 4 preceding weeks for the average comparison, and a hand-seeded *previous* week's (2026-W36) review with 3 focus items (2 unchecked, 1 checked) to test carry-over against a real prior row rather than an empty one. Confirmed on Playbook: expand/collapse accordion behavior; adding a rule, typing into it, and having it survive a reload; retiring a model live (notice appears immediately in both the collapsed row and, since it was the expanded one, the expanded card too) and reactivating it back; every stat tile's numbers by hand against the seed data (net R, win rate, trade count, avg R) for a model spanning both current- and prior-week trades. Confirmed on the Dashboard specifically (not just mobile home's own differently-curated card) that "Performance by model" renders in Playbook's exact `sortOrder`. Confirmed on Weekly review: the header's week label and range; all four stat cards' values and sub-lines, hand-computed from the seed and matching exactly, including the "avg +1.2R" figure (which only lines up once you account for one seed trade landing just outside the 4-week window — the averaging function correctly ignored it rather than the test being wrong about what it was checking); the day-by-day bars' relative heights and weekday captions; best/worst trade tiles; tag frequency's bar widths and dark/weak split; "Copy last week's notes" pulling the exact seeded prior-week text; "Save review" persisting across a reload; and — the one genuinely load-bearing check — that only the 2 *unchecked* focus items from the seeded previous week carried into the new week's checklist, with the checked one correctly absent, confirmed by reading the actual database row `getOrCreateWeeklyReview` created rather than trusting the rendered UI alone. Checked both screens at a real sub-900px viewport too (Korean copy, single-column/2×2 collapse, `BottomTabBar` present with the correct — or on Weekly review, correctly absent — active tab). `npx tsc --noEmit`, `pnpm lint`, `pnpm test` (213 passed), and `pnpm build` all clean throughout. Test user, account, models, trades, and both weekly-review rows deleted afterward.

## Phase 8 — Capital

Four questions were put to the user before building; all four recommended options were confirmed.

**1. Risk-setting history is stored (`account_risk_changes`), so past 1R never gets re-projected.** `accounts.risk_*` only holds today's setting. Before this change, `balanceSeries` computed every past point's 1R under that one current setting, so switching 1% → 2% would have redrawn March's 1R as $300 instead of the $150 actually in force. Trade rows were never at risk either way (`rValueAtEntry` is frozen), but the chart and the "1R history" list would have rewritten history on screen. Migration `20260911120000_account_risk_changes.sql` adds the table (RLS `owner_only`, same as every other table). Its only writers are two triggers on `accounts`: one row on insert (effective from `started_at`) and one per real change to `risk_mode`/`risk_percent`/`fixed_risk_amount` (effective `now()`). The app therefore can't forget to write history, and history can't drift from the account row. Existing accounts were backfilled from their current setting, which is safe because nothing before Phase 8 could change it. This is input history, not a derived value — the balance series, 1R series and ledger are all still selectors. A change applies to every balance point dated on or after its UTC date (`riskSettingOn`).

**2. Drawdown measures trading losses only; the peak moves with cash.** `drawdownState` used to take the raw balance peak, which meant withdrawing profit showed up as a drawdown and could trip the guard. Now each withdrawal lowers the peak by the amount taken out and each deposit raises it by the amount put in. Trades lift the peak whenever the balance makes a new high. A deposit in the middle of a drawdown keeps the dollar loss but shrinks its percentage.

**3. Cash movements can be deleted, not edited.** Click a cash row in the ledger to get a confirm modal (a sheet on mobile). To fix a typo, delete the entry and record it again. Deleting a deposit is refused when some later point would drop below zero, typically because a withdrawal was funded by that deposit (`checkCashMovementDeletion`). Deleting a withdrawal is always allowed.

**4. Only the drawdown limit is editable on Capital.** You edit it in place from the guard card's own "Limit −10%" caption. Starting capital and start date are read-only here and move to Settings in Phase 9.

Smaller calls, none of them spec'd:

- **The ledger is still a selector with nothing stored.** `ledger()` now also emits the opening balance as its oldest row, built from the account and drawn as mock 3a's "Deposit · Starting capital". Each row carries `rValueBefore`/`rValueAfter` under the setting in force on its date, which is where mobile's "1R $266→$316" comes from. `filterLedger` treats the opening row as cash. CSV export (`/capital/export?filter=`) re-derives the same rows through the same selector and copy module (`ledger-copy.ts`).
- **Withdrawal cap = `availableBalanceOn(date)`**: the lowest balance from the withdrawal's slot onward, not just today's balance. Without this, a backdated withdrawal could pass today's check and still overdraw an earlier point. Cash dates are bounded to `[started_at, today]`. The client blocks both cases with localized copy, and the server action re-checks.
- **The modal preview is today's view.** "Balance after" and "1R moves to" describe the balance the account will have and the 1R the *next* logged trade freezes, because that is all recording cash actually changes. The mock's caption ("Only trades logged after this date use the new 1R") is kept when the date is today. For a backdated entry it would be false (trades logged between that date and now keep their old 1R), so a variant caption says so instead.
- **The chart's x-axis is real time**, per README's "on the same time axis". Each cash movement contributes a before point and an after point on the same date, which draws the step README asks for. The old index-spaced chart couldn't draw a vertical step. A risk change steps the 1R line the same way but gets no dot or caption, since README only names cash events there. Captions sit under their own x position, and one closer than 14% of the width to an already-placed caption is dropped, so dense months don't overlap. Start and end captions are always kept.
- **"1R history" lists the latest 6 milestones** (start, each cash movement, each risk change, today) labelled "Mar 3 · $15,000". The mock's month-only "Mar" becomes ambiguous as soon as two milestones share a month. Risk-change rows name the new setting ("Oct 1 · 2%").
- **Changing risk always goes through a confirmation**: README's "warn on change". It shows today's 1R → the next trade's 1R and states that the N trades already logged keep theirs. Picking Fixed opens an amount field prefilled with today's 1R, and re-picking Fixed is how that amount gets edited. `updateRiskSetting` writes only the account row and never reads or writes a trade.
- **Type scale gains 34 / 40 / 52px.** The difference from Phase 3's 28px → 30px rounding is the source. That 28 came only from the canvas, while README § Capital itself gives "800 34px", "800 40px" and "800 52px" for this screen, and README calls its px values intentional. Canvas-only values were still rounded: 19px modal title → the Modal primitive's 17px, 7px tag radius → 8, `#fbfcfd` cash-row tint → `surface-faint` (`#fafbfc`, the same substitution Trade log's row hover already made).
- **The "1R moves to" value is accent blue**, an exception to the accent-only-for-actions rule. README itself colours 1R `#3182f6` everywhere it appears as a series (the 1R line, the legend swatch, mobile's growth bar), so the value text follows that series identity rather than reading as a P&L loss.
- **Nav order stays Dashboard / Trades / Calendar / Playbook / Capital** (build prompt §7, README's "fifth nav item"). Mock 3a draws Capital before Playbook, but canvas order loses to README.
- **Mobile past the mock:** 3b-mobile stops at 최근 내역. The risk card (compact: the 1R card above already shows today's value) and the drawdown guard follow underneath in the same vocabulary, because both are safety controls a phone-only session still needs. 전체보기 expands the list in place with the All / Cash / Trades filter and CSV export rather than routing somewhere new.
- **Dashboard warning appears only once the limit is crossed** (README: "crossing it should surface a warning on the dashboard"). The trade form already warns from 2 points out. It is built from the trade form's warning row (an accent chip plus a line of copy), since the tokens have no danger colour and the loss blue must not double as one.
- **Capital with no account** reuses Phase 4a's `AccountSetup`, so there is still exactly one place an account row gets created.
- **Not done, flagged:** README § Capital's "every surface that shows money also shows R, and vice versa". Capital's own surfaces show both. Trade detail, Dashboard and Trade log remain R-only. Adding currency there is outside Phase 8's listed scope in the build prompt and was left for a later decision rather than widened into here.

**Three bugs found by using the screen rather than reading it**, all fixed before commit:

- **A blocked withdrawal still previewed its impossible result** ("Balance after −$6,182", with a hyphen rather than the app's minus). The preview is now withheld, showing "—", while the amount exceeds what's available.
- **"1R history" repeated today's row** after cash was recorded dated today: the deposit milestone and the closing "today" point landed on the same date. `capitalSeries` now skips the closing point when a milestone already sits on that date, and there is a regression test for it.
- **Longer notes truncate in the desktop ledger's entry column** (the mock's fixed column widths plus a 5-character FX symbol). Truncation is kept for fidelity, and the cell carries the full text as a `title` attribute.

**Verified live** against the real Supabase project with a disposable user whose data was seeded to mirror mock 3a: $15,000 opening on Mar 3, deposits May 1 and Sep 1, a Jul 15 withdrawal, eight FX trades with each `r_value_at_entry` frozen at 1% of the running balance, and one open trade. The migration was pushed first. Account creation wrote the opening `account_risk_changes` row through the trigger, confirmed by query.

The login itself was done by the user in the browser, since the automation doesn't type passwords.

**Desktop, 1400px:** every hero, stat, ledger and guard figure matched the seed. The chart stepped at each cash event, with dots and "May · deposit / Jul · withdrawal" captions. The modal previewed "$28,818 / 1R $288 from $238" for a $5,000 deposit.

Withdrawal blocking:
- A $30,000 withdrawal was blocked against the $23,818 balance.
- Backdated to Jul 20, the cap became $18,225 (the Aug 5 low, not today's balance): $18,300 was blocked, $18,200 allowed, and the backdated caption appeared.

The rest of the flow:
- **Recorded a real $1,000 deposit:** the ledger row, "4 in · 1 out", 1R $248, and the guard's peak moving with the cash ($25,304, still −1.9%) all showed up. Every trade's `r_value_at_entry` and `updated_at` were then byte-identical to a pre-test snapshot.
- **Deleted that deposit** through the ledger modal.
- **Switched risk 1% → 2%** through the warning modal ("$476 from $238", "The 8 trades already logged keep…"):
  - DB: a second history row was written by the trigger, and the trades were still identical to the snapshot.
  - Screen: the history kept $150 / $203 / $184 / $239 and added "Sep 11 · 2% · $476 / R".
- **Edited the drawdown limit in place to 1.5%:** the card read "Limit reached", the Dashboard showed the warning card, and New trade warned "Drawdown is 2.0% against a 1.5% limit".

**Mobile, 500px CSS width (ko), no horizontal overflow:** hero with principal/P&L chips and an ink sparkline. The 1R card read "잔고의 2% · 3월 $150에서 시작" with its two-tone bar and "$150 / +218% / $476". Recent activity showed the "1R $189→$239" shift on cash rows. 전체보기 expanded in place with the filter. The compact risk and guard cards, the 입출금 sheet, and the dashboard warning card all rendered.

The ledger CSV export (cash filter) was checked by server-side fetch and matched the ledger rows. `npx tsc --noEmit`, `pnpm lint`, `pnpm test` (232 passed) and `pnpm build` are clean. The test user and all of their rows (account, trades, cash movements, risk history, models) were deleted afterward and confirmed gone.

## Phase 9 (part 1) — dollar alongside R, empty states, Settings

Three of Phase 9's six items: the rest (PWA manifest/icons, a keyboard-focus pass, and the Vercel deploy writeup) are separate follow-ups per the user's own instruction to land these three, commit, and stop.

### 1. Dollar alongside R

README's own line ("every surface that shows money also shows R, and vice versa") sits under § Capital and was fully built there in Phase 8; nothing elsewhere in the app showed a dollar figure at all before this pass.

- **R stays the primary number everywhere it already was; the dollar figure is a small caption underneath it, never a replacement.** Added to exactly two places, both explicitly named in the ask: the Dashboard hero ("Month to date", desktop and mobile) and Trade detail's "Realized" headline. Every other R on these two screens (Avg win/loss, per-model rows, session tiles, chip counts) stays R-only — retrofitting a dollar figure onto every row of every screen (the trade log table, the calendar's day cells, weekly review's day-by-day bars, playbook's stat tiles) would be a much larger redesign of screens already fidelity-checked against their mocks, and wasn't what was asked; flagged here rather than silently expanded into.
- **The dashboard's dollar figure is `tradingPnL(trades)`** (Phase 8's own capital selector, already correct for summing `pnlAmount` over an arbitrary trade list) over the same month-scoped trades the R figure already uses — not a new calculation. Trade detail's is `pnlAmount(trade)`, the existing per-trade selector. Both format through the existing `formatSignedCurrency`.
- **Where the account's currency comes from**: `page.tsx` (dashboard) and `[id]/page.tsx` (trade detail) already fetch the account for the drawdown guard: its `currency` is threaded down as one more prop. The dashboard's account-less path uses `"USD"` as a fallback that's never actually rendered (the empty state replaces the whole hero).

### 2. R display precision, wired for real

Not asked for directly, but exposed once Settings needed to make `r_precision` do something: before this pass every `formatR` call site had its own hardcoded default (`1`), and the column existed in the database but nothing ever read it back — the exact shape of problem this project has previously resolved by deleting the setting outright (`settings.locale`, Phase 3), on the reasoning that "an unused setting left sitting in the schema just invites confusion." `r_precision` is not permanently unwireable the way `locale` was, so the fix here is to wire it rather than drop it.

- **`src/lib/settings/context.tsx`**: a `SettingsProvider` seeded from a server read, the same shape as `LocaleProvider`'s cookie — except there's no "guess then correct" problem to solve (the server always knows the real saved value, unlike a first-time visitor's viewport width), so it's a plain context with no local state or resize listener. `useFormatR()` returns `formatR` bound to `settings.r_precision`, so a call site that doesn't pass its own `precision` follows the user's preference and one that does (e.g. expectancy's fixed 2 decimals, avg win/loss's fixed 1) is untouched.
- **`pnl_convention` needed no client plumbing at all.** Every gain/loss color already flows through a CSS variable pair RootLayout re-points via `[data-pnl]` on `<html>`; no component branches on the convention's name in JS, so `RootLayout` sets the attribute server-side from `getSettings()` and that's the entire wiring.
- **Rewiring the 40 existing `formatR` call sites was mechanical, not a rewrite**: since almost every call site already reads as bare `formatR(value)` (relying on the old hardcoded default), swapping the plain import for `const formatR = useFormatR();` inside each component reproduces the exact same call sites unchanged. 17 insertions across 13 files — one per component that calls `formatR`, including small leaf components a first grep pass missed (`Panel` in weekly-review-view.tsx, which takes `t` as a prop rather than calling `useT()` itself but is still an ordinary function component free to call its own hooks).
- **`getSettings()` runs on every request via RootLayout**, including `/login`, where there's no session. Confirmed safe rather than assumed: RLS-blocked reads return zero rows, not an error, so `.maybeSingle()` resolves to `null` and the layout falls back to `kr`/`1`.
- **A Settings save calls `router.refresh()` and the action calls `revalidatePath("/", "layout")`** — both, not either — since a settings change has to reach `RootLayout` itself (the one place `data-pnl` and the `SettingsProvider` value are set), not just the current route segment.

### 3. Settings screen

No mock exists for `/settings` (README's own open-questions list names it explicitly as undesigned) — built from the same card vocabulary as every other screen, one `Card` per group, `Segmented`/`Combobox`/`Field` for the controls, a single "Save settings" action gated on the form actually being dirty.

- **`pnl_convention`, `default_instrument`, `default_session`, `r_precision`**, exactly the four fields the build prompt's `settings` table lists as user-facing (`locale` was dropped in Phase 3 and stays dropped). `default_instrument`/`default_session` are the same `Combobox`/`Segmented` primitives New trade's own Context section uses, and already worked the moment they were wired — `trades/new/page.tsx` was already reading `settings?.default_instrument`/`default_session`, just from a row nothing ever wrote to.
- **R precision is a 3-way `Segmented` (0/1/2), not a free-typed number**, even though the database's own `check (r_precision between 0 and 4)` allows up to 4. Three or four decimal places on a value that's fundamentally a small multiple (2.8R, −1.0R) has no realistic use; narrowing the *UI* is a display choice, not a data-integrity one, so `updateSettings` still validates the full 0–4 range server-side rather than assuming the client only ever sends 0–2.
- **The P&L convention card previews both sample chips live**, reading the *picked* (possibly unsaved) convention's CSS variables directly by name (`--pnl-gain-kr`/`--pnl-gain-west`, etc.) rather than through the `text-gain`/`bg-gain-tint-1` utility classes every other gain/loss color in the app uses — those only ever reflect the *saved* convention via `[data-pnl]`, which is exactly the state this preview needs to show ahead of.
- **Settings writes via `upsert`, not `update`.** `settings.user_id` is the PK and always seeded by `handle_new_user` (Phase 1), so this is normally an update — but an `update().eq("user_id", ...)` against a row that somehow doesn't exist would silently match zero rows and report success without saving anything. `upsert` makes that edge case self-healing instead of silently lossy.
- **Not one of the five `NAV_ROUTES`** — same precedent as Weekly review (Phase 7): README's Nav section only ever lists the five, so Settings is reached by a link rather than a sixth tab, and `TopBar`/`BottomTabBar` render with no item active on this screen either.
- **Desktop reaches it via a "Settings" text link in the Dashboard's `TopBar`**, right beside the existing "Weekly review" link (identical styling, same precedent). **Mobile has no per-screen header action on any other screen** (`SignOutButton` was only ever mounted on desktop's `TopBar`; mobile's only account-related control was the Dashboard header's avatar circle, which *was* a direct sign-out button) — reworked so the avatar now links to `/settings`, and Sign out moved into an "Account" card at the bottom of the mobile Settings screen. This is a real behavior change flagged here rather than folded in silently: mobile sign-out now takes one extra tap. Desktop's `SignOutButton` stays exactly where it always was, on every `TopBar` including Settings' own.

### Calendar's missing "no account" empty state (found while auditing task 2's own"empty states" scope)

`DesktopCalendar`/`MobileCalendar` both already declared a `hasAccount` prop but never read it — a fresh install landing on `/calendar` before ever setting up an account rendered a plain, fully-empty month grid with no explanation and no way to start, the one screen out of six that never got this in Phase 5 (Dashboard, Trade log, Playbook, Weekly review, and Phase 8's Capital all did). Added the same `EmptyState` + "Log your first trade" CTA every other screen uses, in the same position (replacing the grid + summary row on desktop, the whole scrollable body on mobile) while keeping the header chrome (`TopBar`/month stepper on desktop, the mobile header) visible throughout, matching how Dashboard's own account-less branch is built.

**Verified live** against the real Supabase project, in a real browser, with a disposable test user: confirmed accountless via server-rendered fetch that both `/calendar` locales (en/ko) now render "Log your first trade"/"첫 트레이드를 기록하세요" instead of an empty grid, and that `/settings` renders its real defaults (`EURUSD`/`asia`) before any save. Then added an account and one EURUSD trade (entry 1.1005, stop 1.1000, exit 1.1012 → +1.4R, `rValueAtEntry` $150) to the same user and, logged in through the real browser:
- Dashboard hero read "+1.4R" / "+$210" (1.4 × $150), matching the seed by hand.
- Trade detail's Realized card read "+1.40R" / "+$210" once R precision was changed to 2 (see below) — same trade, same math, both screens agree.
- On `/settings`: switched P&L convention to West (the preview chips flipped from red/blue to green/red immediately, unsaved), changed the default instrument to GBPUSD and session to London, set R precision to 2 (preview "+18.40R"), and saved — the button showed "Saved.", and every check below happened on a real page load afterward, not a client-only state change:
  - `document.documentElement.dataset.pnl === "west"` on a fresh navigation.
  - Dashboard, Calendar, and Trade detail all re-rendered in green/red with two decimal places ("+1.40R", "+$210"), including `Avg win`'s own fixed-precision "1.4R" staying at one decimal — confirming call sites with an explicit precision argument are correctly unaffected by the setting.
  - `/trades/new` opened with Instrument pre-filled `GBPUSD` and Session pre-selected `London`.
- Resized to a real mobile width (500px CSS, Korean): the dashboard hero showed the same "+1.40R"/"+$210" in green; tapping the header avatar circle navigated to `/settings` (not a sign-out); the mobile Settings screen rendered the same four cards in Korean plus the new "계정" card, and its "로그아웃" button was present and reachable.
- Confirmed the desktop `TopBar`'s "Settings" link (next to "Weekly review") navigates correctly.

`npx tsc --noEmit`, `pnpm lint`, `pnpm test` (232 passed), and `pnpm build` all clean throughout. The test user, its account, trade, and settings row (cascade-deleted with the user) were removed afterward and confirmed gone.

## Phase 9 (part 2) — PWA, keyboard focus, Vercel setup

### 4. PWA — manifest and icons

No rasterizer (ImageMagick/rsvg/etc.) is available in this environment, and running `next/og`'s `ImageResponse` in a standalone script (rather than inside Next's own build/request pipeline) isn't reliable — its compiled Satori/resvg internals rely on Next's own bundling. So the icon mark is one shared component (`src/lib/app-icon.tsx`, a plain ink tile with a white "M") rendered through Next's own file-convention routes, which run `ImageResponse` correctly because Next itself executes them:

- `src/app/icon.tsx` (32×32) and `src/app/apple-icon.tsx` (180×180, unrounded — iOS applies its own squircle mask and expects a flat square underneath it) are Next's special favicon/apple-touch-icon conventions: auto-detected, auto-linked into `<head>`, no `metadata.icons` entry needed.
- `src/app/manifest.ts` is the matching file-convention manifest — auto-served at `/manifest.webmanifest`, auto-linked, `display: "standalone"` is what makes "Add to Home Screen" open without browser chrome.
- The manifest's 192/512 sizes aren't covered by either special convention, so they're two plain Route Handlers (`src/app/icon-192.png/route.tsx`, `.../icon-512.png/...`) that call the same `ImageResponse`+`AppIconMark` pair directly, with an explicit `immutable` cache header since the bytes never change.
- The 512px icon is also listed with `purpose: "maskable"` — the glyph sits at ~56% of the canvas, well inside the ~80% safe zone Android's own mask can crop to.

**A real bug, caught only by fetching these routes without a session**: `/icon`, `/apple-icon`, and `/manifest.webmanifest` have no file extension, so the middleware's existing "skip auth for image extensions" rule (`.svg|.png|.jpg|...`) never covered them — an unauthenticated visitor (which is exactly who requests a favicon and a PWA manifest: before ever logging in, and mid-"Add to Home Screen") got redirected to `/login` instead of the actual icon/manifest, silently breaking both features for anyone not already signed in. `src/middleware.ts`'s matcher now excludes these three exact paths by name, verified by fetching all five icon/manifest routes with no cookie at all and confirming 200 + the right content-type, not a redirect.

### 5. Keyboard focus traversal — found real gaps, not just a read-through

Grepped every raw `<button>`/`<a href>`/`<Link>` in the app against `focus-visible` occurrences in the same file, rather than eyeballing — the established convention (`focus-visible:ring-2 ring-accent ring-offset-2`, used throughout `Button`/`Modal`/`TopBar`) is followed almost everywhere a component is built on the shared primitives, but a long tail of hand-rolled buttons and links (nav Links, pager buttons, sort headers, calendar month-nav, filter pills, focus-item checkboxes, "Copy last week's notes", export links...) across some dozen files never got it. All of them now carry the same ring — mechanical, but real: without it, a keyboard user tabbing through these controls saw nothing (not literally invisible, since nothing in this app strips the browser's own default outline, but visually inconsistent with everything else, and thin/easy to lose against these backgrounds). Two settings-related Links (Dashboard → Settings, Dashboard → Weekly review) added earlier this same session were themselves missing it.

**Three real, functional bugs found**, not just missing polish:

- **`Modal` had no keyboard focus trap and never restored focus on close.** Every dialog in the app (delete confirmations, the cash-movement modal, the risk-change confirmation, account setup, import, the attachment lightbox, the calendar day-detail modal — all built on this one shared component) let Tab walk straight out of an open dialog into the page underneath it, and closing one dropped focus to `<body>`, so the very next Tab press resumed from the top of the page instead of wherever the user had been. Fixed once, in the shared component: the existing `useEffect` (already scoped to `open`) now also remembers `document.activeElement` on open, intercepts Tab/Shift+Tab to cycle only through the dialog's own focusable elements, and restores focus to that remembered trigger element on close. Fixing it in one place fixed it for every dialog in the app at once. **Verified live**: opened Trade detail's delete confirmation, tabbed forward through panel → Close → Cancel → Delete → wraps back to Close, Shift-tabbed backward (Close → Delete), pressed Escape, and confirmed focus landed back on the "Delete" button that opened it — visible ring included.
- **Trade log's "clear filter ×" was a `<span role="button" tabIndex={0}>` nested inside a `<button>`** (invalid HTML — a button can't contain another interactive element) **with only an `onMouseDown` handler, so Enter/Space did nothing** — a keyboard user could tab to it but never actually clear a filter. Restructured into two sibling `<button>`s sharing one visual pill (the fill/radius moved from the button onto a wrapping div) rather than one button nesting a fake one. **Verified live**: tabbed to a "Sweep side" pill already filtering `?sweep=low`, tabbed once more onto "Clear Sweep side", pressed Enter, and confirmed the URL dropped back to a bare `/trades`.
- **The same file's dropdown option buttons only had `onMouseDown`, no `onClick`/`onKeyDown`** — tabbable, but Enter/Space silently did nothing there either (the mousedown-not-click pattern is deliberate, so a click doesn't blur-race the container's auto-close timer, but it left keyboard users with no equivalent path). Added a parallel `onKeyDown` for Enter/Space that calls the same `toggle()`, independent of the mouse path. **Verified live**: opened the Sweep side dropdown via click, tabbed onto "Low purged" (visible ring), pressed Enter, and confirmed the URL picked up `?sweep=low` and the dropdown closed — the exact interaction a mouse user gets, now available to a keyboard user too.

Also fixed alongside, smaller but real: Playbook's model-name field was itself wrapped in a `<button onClick={onCollapse}>` — another nested-interactive-element case, and since the input was the only focusable thing inside it, there was no way to collapse an expanded model card by keyboard at all. Split into a plain (unwrapped) name input plus a separate, explicit `ChevronUp` icon button with its own `aria-label`, mirroring the chevron `CollapsedModelRow` already draws for the opposite direction. A few icon-only buttons picked up missing `aria-label`s in the same pass (the attachment lightbox trigger, the weekly-review focus-item checkbox, pager prev/next).

**Not touched, and not a bug**: `Combobox`'s own option buttons rely on `Tab` reaching them for a mouse user (`activeIndex` is what drives keyboard selection, via the input's own `ArrowUp`/`ArrowDown`/`Enter` handling — ­already correct, already tested live in Phase 4c) — ­the option buttons themselves are mouse-only targets by design, not a gap.

### 6. Vercel deployment — setup docs only, no deploy performed

`docs/setup.md`'s "Deploying" section was a single paragraph; expanded into the actual procedure per the user's request to prepare and document rather than deploy (no Vercel access from this session regardless). Confirmed nothing in `next.config.ts` needs Vercel-specific config (bare default) and that the app deliberately avoids `next/image` for attachments already, so there's no `images.remotePatterns` to add either.

- **Exactly three env vars belong on Vercel** — the same three `.env.local` already has for `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY`. Called out explicitly that `SUPABASE_DB_PASSWORD` must **not** go there — it's a real Postgres credential that only this doc's own local CLI commands (`supabase db push`, `gen types`) ever read; the deployed app never opens a direct Postgres connection.
- **Migrations are not part of Vercel's build** — `supabase db push` has to be run by hand (§3, already documented) before or alongside any deploy whose code depends on a new column/table, since nothing in the Vercel pipeline runs it automatically. Flagged as the one real gotcha in an otherwise default Next.js deploy.
- **Post-deploy checklist added**, including confirming the manifest/icons resolve on the live domain without a redirect — the exact bug §4 found and fixed, worth checking again once it's actually running behind Vercel's own edge/CDN layer rather than only `localhost`.

**Verified**: `npx tsc --noEmit`, `pnpm lint`, `pnpm test` (232 passed), and `pnpm build` all clean. Icon/manifest routes were fetched directly (no cookie) against a local server and confirmed 200 with correct content-types; the Modal and FilterDropdown fixes were exercised in a real browser with a disposable Supabase test user (account, one trade, deleted afterward) using real Tab/Shift+Tab/Enter/Escape key presses — not just read from the code.

## Backtest data entry — three real problems, found and fixed

Raised by the user while planning to backtest: enter a full year of EUR trades, then a full year of GBP trades, into the same account.

### 1. Dashboard's default month

**Confirmed the report**: the Dashboard always defaulted to the real calendar month (`currentIsoMonth()`), so an account whose most recent trade is months in the past (a backtest, or a live account you haven't touched in a while) landed on an empty "no trades this month" screen even though real data exists one query away.

**Fix**: `src/app/page.tsx` now defaults to the most recent trade's month (`getMostRecentTradeDate`, a targeted one-row query rather than fetching every trade to find a max — matters once an account holds a year-plus of backtest rows) and only falls back to today's real calendar month when the account has no trades at all, which is exactly when the existing "Log your first trade" empty state is the right thing to show. No other screen was touched — Calendar already lets you navigate months and reasonably defaults to today's; the user's report was specifically about Dashboard.

### 2. Account creation + switching

Confirmed: `AccountSetup` only ever rendered behind a "no account yet" gate, and nothing in the app could create a *second* account or choose which one was active — `getPrimaryAccount()` always meant "the oldest one," full stop, even though README's own data model already reserved `accountId` everywhere for this.

- **`accounts.kind` (`live` | `backtest`, default `live`)** — a real column, not a UI-only label, since the backtest 1R fix below needs a durable way to know which rule an account uses. Confirmed as needed rather than asked as an open question, since problem 3 could not be solved without some such flag.
- **A `current-account-id` cookie** (`src/lib/current-account.ts`) is the one new piece of state. `getCurrentAccount()` (replacing `getPrimaryAccount()` at all 11 of its call sites — page.tsx/route.ts files across Dashboard, Trades, Calendar, Playbook, Weekly review, Capital, New trade) reads it, falls back to `getPrimaryAccount`'s original "oldest account" behavior when it's unset or points at an account that's gone or isn't this user's (RLS makes that read back null, not an error — a stale cookie can't break anything). Zero behavior change for anyone who's never switched. `setCurrentAccount` (a server action) re-validates ownership before writing the cookie and revalidates every account-scoped route.
- **Trade detail is deliberately the one exception** — it now fetches `getAccount(trade.accountId)` instead, so a trade's own page always reflects *its* account's currency/drawdown state regardless of which account happens to be switched on elsewhere. This was already a latent bug (a trade could belong to an account other than "the oldest one" the moment a second account existed) that multi-account switching would otherwise have made newly reachable.
- **UI**: `AccountSwitcher` (`src/app/capital/account-switcher.tsx`) — a compact trigger (name + kind chip) in Capital's header, desktop and mobile, opening a popover of every account plus "+ New account" at the bottom. `AccountSetup` gained a `context: "first-run" | "additional"` prop so the exact same form (now also carrying the `kind` Segmented, and — only for `kind: "backtest"` — a "Start date" field, since a backtest's own start date has to predate the earliest trade being backfilled for the balance timeline to mean anything; a live account keeps defaulting to today, unchanged) serves both the original first-account gate and Capital's "create another" flow. Creating an account from Capital switches onto it immediately, since that's plainly why it was just created.
- Capital's `CapitalData`/`use-capital-summary.ts` gained `allAccounts`; `capital/page.tsx` fetches it via the new `getAllAccounts()`.

### 3. The backtest 1R bug — confirmed, then fixed for backtest accounts only

**Confirmed exactly as described**, both by reading `createTrade` and by driving the real form end to end (not a synthetic test): `rValueAtEntry` was computed via `currentRValue`, which sums *every trade currently stored for the account*, in chronological order by date — not "trades dated on or before this one." Logging a trade doesn't care when it was inserted relative to other rows, only what's already in the table. Entering a full year of EUR first, then GBP, means every GBP trade's freeze-time balance already includes the *entire* EUR year, including EUR trades dated months after that GBP trade — inflating its 1R for money it, by date, hadn't made yet.

**Fix, scoped to `kind: "backtest"` accounts only — a live account's freezing rule is untouched, not merely unchanged in effect**: `createTrade` (`src/app/trades/new/actions.ts`) branches on `account.kind`. `backtest` accounts use a new selector, `rValueAsOfDate` (`src/lib/domain/capital.ts`), which walks the already-chronological `balanceSeries` and keeps the balance from the last point dated on or before the trade's own date — ignoring anything dated after it, however long it's already been sitting in the table. `live` accounts keep calling `currentRValue`, byte-for-byte the original function, on the original code path.

- **The New trade form's "1R today" preview is honest about not being live for a backtest account.** The preview number itself still reads `currentRValue` (today's balance) — recomputing it per-keystroke against the trade's own date field would mean shipping the account's full trade history to the client, real cost for a backtest account holding a year-plus of rows, for a number that's a preview either way (the real one is computed server-side on submit, as it always was). Instead the caption itself changes for a backtest account: "this account freezes 1R to the balance as of this trade's own date, not today's" — so the number on screen doesn't quietly mislead about what's about to be frozen.
- **Same-day trades still net together in entry order**, matching the live rule's own behavior — `rValueAsOfDate` doesn't special-case ties beyond the existing `timeline()` ordering (cash before trades, then `createdAt`), so two trades logged for the same date affect each other the same way `currentRValue` already lets two same-day live trades do.
- **A known, accepted limitation, not silently swept under**: this fixes the user's actual plan (one instrument's full date range, then the next), but backfilling a trade *earlier* than ones already entered still won't retroactively correct those already-frozen values — the same "frozen once, never rewritten" principle every other part of Capital already lives by (recording cash, changing risk %). Noted in the Start-date field's own hint text and here, not treated as a gap to design around.

**Verified live**, with a disposable test user, through the real `createTrade` action (not a DB-level shortcut): created a `backtest` account, starting capital $10,000, start date 2025-09-01. Logged a EUR trade dated 2025-10-05 (+14R) — stored `r_value_at_entry: 100`, correct (nothing precedes it). Logged a second EUR trade dated 2026-08-20 (+14R) — stored `114`, correct (1% of the post-first-trade balance, $11,400). Logged a GBP trade dated 2025-09-05 — *before* both EUR trades by date, but entered *last*, after both already existed in the table — and its stored `r_value_at_entry` was `100`, not the `130` the old (still-correct-for-live) `currentRValue` path would have produced from the already-inflated $12,996 total. Confirmed via direct query against `trades`, ordered by date, immediately after each submission — not inferred from the UI. Also confirmed end to end: Capital's account switcher listed both accounts with correct kind chips; creating and switching to a second `live` account instantly emptied Dashboard/Trades (a fresh account, genuinely no trades) and switching back restored all three backtest trades; Dashboard on the backtest account defaulted to "August 2026" (the latest trade's month), not the real "September 2026."

`npx tsc --noEmit`, `pnpm lint`, and `pnpm test` (235 passed, 3 new covering `balanceAsOfDate`/`rValueAsOfDate`) all clean. The test user, both its accounts, and all trades were deleted afterward and confirmed gone.

## Dashboard period picker

Raised by the user directly off the backtest work above: the Dashboard was hard-locked to "this real calendar month," no `‹`/`›`, no way to look at anything else — fine for a live account trading month to month, unusable for a backtest account holding a year or more of history nowhere near today.

### Presets

Proposed and used six: **This month / Last month / Last 3 months / This year / All time / Custom range**, plus `‹`/`›` as the baseline month-stepper. Reasoning for the set: the user's own examples (이번 달/지난 달/최근 3개월/올해/전체/직접 지정) already covered the two shapes of need — quick recency windows for a live trader reviewing the last few weeks, and wide-open ranges for a backtest run — so nothing further was added on top.

### URL semantics: rolling vs. frozen, same split as Trade log's own date filter

Split into two kinds by design, matching the convention the user explicitly asked to mirror:

- **Rolling** (`this-month`, `last-month`, `3m`, `ytd`, `all`) carry only `?range=`, no dates — resolved fresh against *today* on every load, so a bookmarked "This year" always means the current year, not a snapshot frozen to whenever it was first clicked.
- **Frozen** (`month` via `‹`/`›`, `custom` via the date-input sub-form) carry concrete values in the query (`?range=month&month=2026-08`, `?range=custom&from=…&to=…`) — exactly Trade log's own `from`/`to` convention, unset state kept out of the query string entirely so the smart default keeps working for a plain `/`.

`‹`/`›` only have a well-defined "one month over" for kinds reducible to a single month (`this-month`/`last-month`/`month`) — implemented as `next/link` `Link`s, disabled (`tabIndex={-1}`, `pointer-events-none`, dimmed) rather than removed for `3m`/`ytd`/`all`/`custom`, so the control stays in a stable place instead of shifting the layout per preset.

**Relabeling, caught mid-build**: the smart-default landing state (no `?range=` at all, resolves to `kind: "month"` against the most recent trade's month) always showed a generic "Net R" hero label even when that month happened to equal today's real month — the common case for an active live trader, and a real regression from mock 1a's warmer "Month to date" wording. Fixed by relabeling `month` as `this-month` whenever the resolved month equals today's, regardless of whether it got there via the default or by `‹`/`›` landing back on today — same data, same wording either way.

### Placement — hero itself untouched

Mock 1a's hero card is fixed to "Month to date"; kept the card's own structure completely unchanged and swapped only its label (`DASHBOARD_HERO_LABELS[period.kind]`) and the chrome around it:

- **Desktop**: replaced the static month-label `<span>` in `TopBar`'s right slot with `<PeriodPicker>` — same visual position, now interactive.
- **Mobile**: added a new compact row (`<PeriodPicker compact>`) under the existing header (title + settings avatar), since that header previously carried no date context at all.

All four recomputation targets (hero R, equity curve, the 4 stat cards, model/session breakdown) and Recent trades needed zero selector changes — `periodStats`/`equityCurve`/`byModel`/`bySession`/`sweepAlignment` already take a plain `trades: readonly Trade[]`, period-agnostic by construction, so fetching the right trade list in `page.tsx` (`getTradesInRange` for every kind except `all`, which uses the existing `getAllTrades`) was the entire recomputation fix.

**Verified live** with a disposable test user (6 trades spanning Feb 2025–Sep 2026) at both widths:

- **Desktop (1300px)**: default month, `‹` stepping to the previous month, the preset popover open/close, "All time," "Custom range" (date-input sub-form → Apply), and "This year" — each confirmed by matching URL query string, correctly enabled/disabled `‹`/`›`, and hero numbers/equity curve/stat cards all agreeing with the seeded data for that range.
- **Mobile (420px, Korean)**: same default-month landing, opened the preset popover and confirmed all six Korean labels render (이번 달/지난 달/최근 3개월/올해/전체/직접 지정), and clicked "최근 3개월" — URL became `?range=3m`, hero showed "+2.3R"/"+$230"/"3건"/"67% 승률" with both arrows disabled, matching the desktop YTD/all-time runs' underlying data exactly.

`npx tsc --noEmit`, `pnpm lint`, `pnpm test` (251 passed, new coverage in `dashboard-period.test.ts`), and `pnpm build` all clean. The test user, its account, and all 6 trades were deleted afterward and confirmed gone.

## Deployed-app performance — region mismatch, plus a real duplicate query

Raised by the user: the deployed app felt noticeably slower than `pnpm dev`. Two real causes, found and fixed.

### Vercel function region vs. Supabase region

Confirmed via the deployed URL's `x-vercel-id` response header before touching anything: `icn1::iad1::...` — the edge entry point was Seoul, but the actual serverless function (where every Supabase query runs) executed in `iad1` (US East, Vercel's default), while Supabase itself is `ap-northeast-2` (Seoul, per Phase 1's setup). Every DB round trip was paying a trans-Pacific hop each way, and a single page load makes several of them in sequence (middleware's `auth.getUser()`, RootLayout's `getSettings()`, the page's own account/data fetches) — so the latency compounds per page, not just once.

Fixed with a `vercel.json` at the repo root: `{ "regions": ["icn1"] }`, pinning the function region to Seoul, colocating it with Supabase. Confirmed on the live production URL after redeploy: `x-vercel-id` now reads `icn1::icn1::...`, and `/login`'s round trip (middleware + RootLayout's `getSettings()` — the smallest page in the app, and the only one testable without a real session) dropped from a consistent ~1.24s to ~450–840ms warm.

### `getSettings()` queried twice in the same request

Audited every server page for sequential-queries-that-could-parallelize and repeated queries. Every page already used `Promise.all` for its independent fetches (Phase 5 onward's own convention), and the remaining sequential chains (`getCurrentAccount()` → a query that needs `account.id`) are genuinely sequential — nothing to parallelize there. The one real duplicate: `RootLayout` calls `getSettings()` on every route (for `data-pnl`/`r_precision`), and `/trades/new` separately calls it again in its own `Promise.all` (for `default_instrument`/`default_session`) — two round trips to the same `settings` row in one request.

Wrapped `getSettings()` in React's `cache()` (`src/lib/supabase/queries.ts`) — request-scoped memoization, not a module-level singleton, so it stays consistent with `createClient()`'s own "fresh client per request" rule. No other duplicate call sites found across the app (only one layout, and every other query is called at most once per page).

**Not done, deliberately**: no cross-request caching (`unstable_cache`/ISR) was added. Every route already goes dynamic from `cookies()`-based auth (Supabase SSR's own required pattern), so route-level caching isn't available without restructuring auth, and for a single-user app the win would be marginal next to the region fix. Flagged rather than silently pursued.

**Verified**: `npx tsc --noEmit`, `pnpm lint`, `pnpm test` (251 passed) clean. Deployed via `git push`, confirmed on the live `x-vercel-id` header and with repeated `curl` timing against `/login` before and after.

## CSV import — backtest accounts can leave `rValueAtEntry` blank

Follow-up to the backtest 1R fix above: importing a full year of backtest data by hand means computing every row's `rValueAtEntry` manually, in date order, threading the running balance through — exactly the kind of arithmetic one wrong row silently corrupts for every row after it. Extended CSV import to do this automatically for `backtest` accounts, on the same `rValueAsOfDate` rule `createTrade` already uses for one trade at a time. `live` accounts are untouched — the column is still required and still goes straight from the row onto the trade, since a historical live trade's 1R genuinely can't be derived from anything the app can compute after the fact.

- **`assignBacktestRValues`** (`src/lib/domain/capital.ts`) is the batch form of `rValueAsOfDate`: given the account's *existing* events (from `timeline()`) plus a batch of not-yet-inserted rows, it walks one merged chronological pass — existing events and batch rows interleaved by date — assigning each row its 1R from the running balance as it goes, then folding that row's own P&L into the balance for whatever comes after it (its own or existing). A row that already carries an explicit `rValueAtEntry` (a CSV that already knows some of its own history) is left untouched but still counts toward the balance for later rows, so a file can freely mix computed and explicit rows.
- **Batch order is not insertion order.** `rows` need not be date-sorted — a hand-authored backtest CSV commonly isn't — so the function sorts by `(date, row index)` internally before walking, and returns results indexed back to the caller's original order. Same-date rows net together in the order given, the same "entry order" rule same-day trades already followed via `createdAt` once they're real rows, applied here to import order instead.
- **A batch resolves every row's value from the *whole* batch's true chronological order, not just what was already in the database when it was entered** — a genuine improvement over the single-trade-at-a-time path's own accepted limitation (a trade backfilled earlier than ones already entered doesn't retroactively correct them). Within one CSV import, order in the file simply doesn't matter; only date does.
- **`csvRowSchema`/`csvRequiredFields`** (`src/app/trades/import/schema.ts`) now take `accountKind`: `rValueAtEntry` drops out of the required-fields list and its own field validation for `backtest` (blank is valid; a value, if given, still has to be a positive number), unchanged for `live`. `ImportModal` threads `accountKind` down from `TradeLogView` (itself from `trades/page.tsx`'s already-fetched `account.kind`) and shows a one-line hint in the mapping step for backtest accounts explaining the column is optional.
- **`importTrades`** (`src/app/trades/import/actions.ts`) fetches the account's existing ledger once (`getAccountLedgerInputs`, already the pattern `createTrade` uses) and calls `assignBacktestRValues` once for the whole batch when the account is `backtest` — one extra query regardless of file size, not one per row. A defensive check before insert (`some(v => v === null)`) catches the theoretical case of an unresolved value with a real error message instead of letting it hit the database's own `NOT NULL` constraint.
- **3 new unit tests** in `capital.test.ts`: unsorted-batch processing (a row appearing earlier in the array but dated later is computed second, and results still come back in the original array's order), an explicit-override row still feeding the running balance for rows after it, and existing trades/cash movements already in the account folding in before the batch starts.

**Verified live**, with a disposable test user: created a `backtest` account (starting capital $10,000, 1% risk, start date 2025-01-01), then imported a 3-row CSV in the exact adversarial shape the original bug report used — two EUR rows (2025-10-05, 2026-08-20) listed *first* in the file, a GBP row dated *before both* (2025-09-05) listed *last* — every row's `rValueAtEntry` cell left blank. The mapping step correctly showed "1R value ($)" as optional with the backtest hint banner; the preview step accepted all 3 rows ("3 ready to import") with no value in that column. After import, queried `trades` directly (service-role, ordered by date):

| date | instrument | `r_value_at_entry` |
| --- | --- | --- |
| 2025-09-05 (GBP) | GBPUSD | `100` |
| 2025-10-05 (EUR #1) | EURUSD | `114.00000000000159` |
| 2026-08-20 (EUR #2) | EURUSD | `129.96000000000362` |

Matches hand computation exactly (float noise aside): GBP first by date, nothing precedes it → 1% of $10,000. EUR #1 next, with GBP's own +$1,400 already folded in → 1% of $11,400. EUR #2 last, with both prior trades folded in → 1% of $12,996 = $14,815 → 1% = $148.15 ≈ the $148/R Capital's own "Risk per trade" card showed live. Capital's ledger view corroborated the same three balances independently. Then created a second, `live` account and re-imported the identical file (still blank `rValueAtEntry` cells): the mapping step marked the column required again, and the preview step rejected all 3 rows with "Enter a 1R value." — confirming the `live` path is completely unchanged.

`npx tsc --noEmit`, `pnpm lint`, `pnpm test` (254 passed), and `pnpm build` all clean. The test user and both its accounts (and all imported trades) were deleted afterward and confirmed gone.

## Six real mobile bugs — found on an actual phone-width viewport, not just reasoned about

The user reported six issues from using the app at real phone width. All six were reproduced first (a disposable test user, one live trade, 390×844 viewport) before touching any code — two turned out to already match the mock and were left alone with the reasoning recorded, not silently "fixed" to look nicer.

1. **Home title duplicated a tab label.** The mobile home header read "기록" (a leftover from before the dashboard-period-picker work replaced its old month-scoped title with something generic) — identical to the Trades tab's own "기록", while the Home tab itself is labeled "홈". Changed the header to "Dashboard"/"대시보드", matching what desktop's own `TopBar` already calls this same route — distinct from both tab labels, and consistent with the one other place this screen is named.
2. **Hero card had a large dead zone below the stat chips.** `Sparkline` only draws a `<polyline>` once `values.length > 1`, but the `<svg>` around it always renders at its full `h-[70px]` regardless — with exactly one trade this period, the account had nothing to draw but the box, plus its `mt-18` margin, still took the space. Gated the whole `Sparkline` (not just its polyline) on `equity.points.length > 1`, so the card's own height simply follows its content instead of reserving room for a curve that isn't there.
3. **Trade log's date-range pill showed only a bare "—" with no label when unset.** A plain empty `<input type="date">` renders its *placeholder segments* from the browser's own configured display language, not this app's `lang` attribute or `t()` — confirmed on a real phone, not assumed: two side-by-side empty date inputs plus a "—" separator, with nothing readable in the unset state. Unified `DateRangePill` to always use the closed-summary-button + popover pattern the desktop side already had (`"All dates"`/`"전체 기간"`, `"Sep 1 – Sep 11"`, etc., built from `formatTradeDate(..., locale, true)` rather than a hardcoded `"en"`), matching every sibling `FilterDropdown` pill on the same row instead of being the one filter with its own special-cased mobile branch. The desktop popover's own native date inputs still show browser-language placeholder text while open (a pre-existing, accepted quirk noted in the component's own comment, unrelated to this fix) — the bug was specifically the *closed*, always-visible state having no readable text at all.
4. **Calendar showed zero day numbers — not "missing on some cells," literally none, anywhere.** `MobileCalendar`'s day cell only ever rendered a `<span>` for the day's net-R value, gated on a trade existing that day; a day with no trades rendered nothing inside its button at all, unlike `DesktopCalendar` which always draws `Number(date.slice(-2))` regardless of whether that day traded. Added the same day-number span to every mobile cell (`text-11 font-semibold`, muted when the day has no value, colored to match the heat style when it does), stacked above the R value instead of desktop's absolute-corner placement, since the 42px mobile cell has no room to spare.
5. **Calendar's four summary stat cards (Trading days / Green days / Best day / Worst day) are desktop-only — checked against the mock, and they're absent there too, not a gap.** Read `docs/design-canvas.html`'s own `1d` mock (the mobile calendar screen inside it) line by line: header with month + `‹›` + a disabled "주간" label, the 7×7 day grid, and a selected-day detail card listing that day's trades — no fourth stat-row anywhere in the mobile screen, on either the "no day selected" or "day selected" state the mock draws. Left as-is; this is the one item of the six that wasn't a bug, so no code changed here — reported per the user's own instruction to check the mock first.
6. **Bottom tab bar wasn't actually pinned to the screen.** It used `sticky bottom-0`, which only pins against a scroll container *taller than the viewport* — on a short page (an empty Playbook, a short Settings screen), there was nothing to scroll against, so the bar rendered in normal flow immediately after the content and left bare page background between it and the real bottom edge of the screen. Changed to `fixed inset-x-0 bottom-0` in the one shared `BottomTabBar` component (not per-page), with a matching `env(safe-area-inset-bottom)` bottom padding for the iOS home-indicator area. Since `fixed` removes the element from normal flow entirely, the component now also renders a same-height invisible spacer div immediately before the real bar — reserves the exact space in flow so the fixed bar never overlaps whatever's actually last on the page (verified: Settings' "로그아웃" button, previously the last thing on that screen, stays fully visible above the bar rather than hidden under it). One fix in one place covers all nine screens that render this component, rather than a bottom-padding value copied into each.

**Verified live** at 390×844 (Korean) with a disposable test user (one live trade, "today"): all four real fixes confirmed by screenshot — the home header now reads "대시보드" against the tab bar's "홈"/"기록"/"캘린더"/"플레이북"/"자산"; the hero card ends right after the win-rate chip with no gap; Trade log's filter row opens on "전체 기간"; the September grid shows every day number 1–30 with the traded day (11) additionally showing "+3.3" beneath its number; Playbook's empty state (the shortest real page in the app) now has the tab bar flush against the true bottom of the viewport with no dead space beneath it. The `DateRangePill` unification was also checked back at 1300px (desktop): "All dates" unset, "Until Sep 11" (to-only), and "Sep 1 – Sep 11" (both set) all render correctly, confirming the merge didn't regress the desktop side it was copied from.

`npx tsc --noEmit`, `pnpm lint`, `pnpm test` (254 passed), and `pnpm build` all clean. The test user, its account, and its one trade were deleted afterward and confirmed gone.

## Bottom tab bar — more breathing room above the safe area

Follow-up to the `fixed`-positioning fix above: `pb-[env(safe-area-inset-bottom)]` alone put the tab labels right on top of the iOS home-indicator strip with nothing between them — enough of the app's own chrome to make the bottom row genuinely mistap-prone, per the user's own report.

- **`BAR_EXTRA_BOTTOM_PADDING = 10`** (`src/components/nav/bottom-tab-bar.tsx`), added *on top of* the safe-area inset rather than replacing it: `paddingBottom: calc(env(safe-area-inset-bottom) + 10px)`. A device with no inset at all (an older iPhone SE shape) still gets the 10px — `env()` simply resolves to `0` there, so nothing is lost, only added. The leading spacer div's height grew by the same 10px so the fixed bar still never overlaps whatever's last on the page.
- **Touch targets and icon/label spacing were already fine, confirmed rather than assumed**: measured live via `getBoundingClientRect()` on a real 390px-wide page — every tab is 100×56.5px, well past the 44px minimum in both dimensions, and the icon-to-label gap is a steady 4px across all five tabs (`gap-4`), matching the tight-but-legible spacing iOS's own system tab bar uses. Nothing needed to change here; this half of the ask was a check, not a bug.

**Verified live** at 390×844: `getComputedStyle` on the nav confirmed `padding-bottom: 10px` in a browser with no safe-area inset (so the full 10px is the added margin, not swallowed by an inset already covering it) and the bar still flush with the true bottom of the viewport (`gapBelowNav: 0`) — pinning is unaffected by the added padding. Re-checked Playbook's empty state (shortest real page) and Settings' "로그아웃" button (previously the last element on that screen) — both still render with the tab bar correctly below them, nothing hidden or left floating.

`npx tsc --noEmit`, `pnpm lint`, `pnpm test` (254 passed), and `pnpm build` all clean. The test user and its account were deleted afterward and confirmed gone.

## The padding bump above didn't actually reach a real iPhone — `viewport-fit=cover` was missing

The user reported the extra 10px above made no visible difference on their own phone, despite the previous fix. The actual cause was one layer up from `BottomTabBar` entirely: `src/app/layout.tsx`'s `viewport` export only ever set `themeColor`, so Next's default viewport meta tag never included `viewport-fit=cover`. Per the CSS Environment Variables spec, iOS Safari only lets `env(safe-area-inset-*)` resolve to anything but `0` — and only lets page content draw underneath the home-indicator area at all — once that's set; without it, Safari reserves that strip itself and the page's own content stops short of it regardless of any padding math done in CSS. The whole `calc(env(safe-area-inset-bottom) + 10px)` fix was computing against a permanently-zero inset on the one platform it exists for.

Added `viewportFit: "cover"` to the `viewport` export. Confirmed via the server-rendered HTML rather than assumed: `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover"/>` now appears on every page (root layout, so this applies everywhere at once). This is the missing prerequisite, not a replacement for the earlier padding change — both are needed: `viewport-fit=cover` is what makes the inset non-zero on a real device in the first place, and the `BAR_EXTRA_BOTTOM_PADDING` is what sits on top of it.

**Not independently verified against a real notched iPhone from this environment** — no iOS device is available here, and Chrome's own desktop dev tools don't expose a non-zero `safe-area-inset-bottom` in emulation either, so `env()` reads `0` in every check this session can run either way, same as before this fix. What *is* verified: the meta tag is now correctly present in the actual server response (the one thing standing between "computed against zero" and "computed against the real inset"), and `tsc`/`lint`/`test`/`build` all stay clean. The user's own phone is the only way to confirm the visual result now differs from before.

## Tab bar top edge — rebalanced once the bottom actually had room

Confirmed on the user's real phone: `viewport-fit=cover` fixed the bottom spacing, but with the bottom now genuinely roomier, the icon read as sitting too close to the bar's own top border by comparison — asked to nudge it down slightly.

Changed each tab item's padding from symmetric `py-8` to `pt-12 pb-8` (`src/components/nav/bottom-tab-bar.tsx`) — a 4px shift, top only, bottom untouched since that side already gets its own breathing room from the nav's `BAR_EXTRA_BOTTOM_PADDING`. Confirmed numerically rather than eyeballed, since this session's browser tooling was intermittently unable to hold a real phone-width window this time (`resize_window` calls landed inconsistently — 2560px, 800px, 390px across successive calls on what should've been the same request; forcing the `viewport-locale` cookie to `ko` didn't stick either, since `LocaleProvider` re-measures real `window.innerWidth` on mount and overrides a guessed cookie value, same as it's supposed to for a real visitor). Once a window did land under 900px and the mobile tree actually rendered, `getBoundingClientRect`/`getComputedStyle` on a live tab item confirmed `padding-top: 12px` (was `8px`) and the icon sitting exactly 12px below the bar's top border, `padding-bottom` and the label-to-bottom gap both unchanged at 8px — matching the change made, not just the class name applied.

`npx tsc --noEmit`, `pnpm lint`, `pnpm test` (254 passed), and `pnpm build` all clean. The test user and its account were deleted afterward and confirmed gone.

## Phase 5 — five user-requested changes: win rate, Avg hold, HTF pairing, Instrument default, editable emotion tags

Five independent asks from the user, taken together in one pass.

### 1. Win rate now excludes break-even entirely — reverses the Phase 2 call

Phase 2 decided win rate = wins / (every trade with a result), i.e. BE sits in the denominator but not the numerator, matching mock 2c's "67%" / "6 / 9". The user asked to flip this: **win rate = wins / (wins + losses)**, BE excluded from both numerator and denominator (`winRate` in `src/lib/domain/stats.ts`).

**Why the reversal:** the mock-matching reading meant a BE-heavy stretch mechanically drags win rate down for a reason that has nothing to do with actually winning or losing more — a trader who scratches out of five setups and wins the sixth clean reads worse ("1/6 → 17%") than one who never scratched and lost five ("1/6 → 17%" too, but for a materially different reason the old formula couldn't distinguish). Excluding BE makes the number answer "of the trades that resolved one way or the other, how many were wins" — the reading the user actually wants from it. `expectancy`, `avgWin`, and `avgLoss` are untouched (their own Phase 2 BE-handling — expectancy includes it, avgWin/avgLoss exclude it — was never in question here and is orthogonal to how win rate's own ratio is built).

**BE count is now shown separately, as proposed and implemented**: Weekly review's Win rate stat card sub-line changed from `${winCount} / ${decidedCount}` to `${winCount} / ${winCount + lossCount}`, with a trailing `· N BE` appended whenever the period has at least one break-even trade (`src/app/weekly-review/weekly-review-view.tsx`, `src/app/weekly-review/page.tsx` — `decidedCount`/`winCount` replaced with `winCount`/`lossCount`/`beCount` computed directly from the trade list, matching the pattern already used there rather than routing through a new `PeriodStats` field the dashboard doesn't need). Nowhere else in the app rendered a win-rate sub-line with a trade count, so no other screen needed a matching change.

**Expectancy's own BE treatment was reconsidered, not changed**: the user asked for a recommendation alongside the win-rate flip. Expectancy stays mean-R-across-every-trade-including-BE, because a break-even trade is a real, non-zero data point about the strategy's edge (it occupied risk and returned ~0R, which is exactly what should pull the average toward 0) — that is different in kind from win rate, which is a *rate over decisive outcomes*, not an average over all outcomes. Flipping win rate to exclude BE doesn't imply expectancy should too; the two are different questions using the same events differently, and expectancy including BE is what a scratch trade's real economic effect requires. Left as-is, both in code and in this reasoning being on record for the next time it's questioned.

Test updated in `src/lib/domain/stats.test.ts`: the existing 6-win/2-loss/1-BE fixture now asserts `6/8 = 75%`, not `6/9 = 67%`; added a case asserting `winRate` is `null` when every decided trade is a BE (previously impossible to hit since BE alone still populated the denominator).

### 2. Trade log's "Avg hold" summary stat removed

`avgHoldMinutes` dropped from `TradeLogSummary` (`src/app/trades/trade-log-view.tsx`) and from the query that built it (`src/app/trades/page.tsx`). The `avgHoldMinutes` selector itself stays in `src/lib/domain/stats.ts` (and its test) — it's a legitimate pure selector that happens to have no caller right now, not dead code tied to this one screen; its doc comment was updated to stop claiming the summary row as its purpose. The per-trade **Hold** input (New trade / Trade detail's edit form) and the Trade detail page's own "Hold" tile are both untouched — this was specifically about the aggregate stat on the log's summary row, which the user said goes unused.

### 3. HTF pairing trimmed from four options to two: `M → W → D` and `W → D`

Before touching the schema, queried the live `trades` table directly for every distinct `htf_pairing` value in use: **`m_w_2d` (12 rows) and `w_2d` (3 rows) — zero rows used `d_h1` or `h1_m5`.** Since nothing unexpected turned up, this proceeded without stopping to ask, per the user's own instruction.

- **New codes**: `m_w_d` (M → W → D) and `w_d` (W → D) — the "2D" step in both original options collapses to a plain "D", and the two rarely-relevant fixed-income/M5-timeframe options are dropped outright rather than kept as unused dead weight in the CHECK constraint.
- **Migration** `20260914120000_htf_pairing_two_values.sql`: drops the old `trades_htf_pairing_check` constraint *first* (a real ordering bug caught on the first push attempt — the `UPDATE` statements originally ran before the constraint drop and were rejected by the very constraint they were trying to make obsolete, since `m_w_d`/`w_d` aren't in the old allowed set; the push failed cleanly with nothing committed, confirmed via `psql` before retrying with the constraint drop moved first), then remaps `m_w_2d → m_w_d` and `w_2d → w_d` on every existing row, adds the new two-value constraint, and repoints the column default from `'w_2d'` to `'w_d'`.
- **Applied to the live database and verified directly via `psql`, not just trusted from the push output**: `select htf_pairing, count(*) from trades group by 1` now reads `m_w_d: 12`, `w_d: 3` (same counts as before the remap, just under the new codes); `\d trades` shows the constraint as `CHECK (htf_pairing = ANY (ARRAY['m_w_d'::text, 'w_d'::text]))` and the column default as `'w_d'::text`.
- **`src/lib/database.types.ts` regenerated** from the live schema afterward (`supabase gen types typescript --db-url ...` — redirecting stdout without also silencing the CLI's own stderr banner corrupted the file on the first attempt, caught immediately by `tsc --noEmit` failing on line 1; regenerated with stderr suppressed).
- **App-side code updated to match**: `HtfPairing` type (`src/lib/domain/types.ts`), `HTF_PAIRING_LABELS`/`HTF_PAIRING_ORDER` (`src/lib/labels.ts`, now exactly two entries, "W → D" as the listed default per the new column default), the zod enum in `trade-schema.ts`, the New trade form's `NEW_TRADE_DEFAULTS.htfPairing` (`w_2d` → `w_d`), the fixtures helper, the `/dev/components` gallery's hand-coded `<option>` list, and `labels.test.ts`'s `resolveLabel` cases (rewritten against the new two-option set). CSV import (`src/app/trades/import/schema.ts`) needed no change — it already resolves free-typed instrument/label text through `resolveLabel(HTF_PAIRING_LABELS, ...)` rather than hardcoding codes, so it picked up the new set automatically.

### 4. New trade's Instrument now defaults to the most recently logged trade's instrument, not `settings.default_instrument`

Chose "most recent trade's instrument" over a separate "last used value" column: the trades table already has the answer (no new state to keep in sync), and a trader working the same pair across several sessions gets the behavior they actually want — the field remembers what you logged last, not a fixed default from Settings that never moves. `settings.default_instrument` and its Settings-screen field are untouched and still used as the fallback when the account has no trades yet.

- **`getMostRecentTradeInstrument(accountId)`** added to `src/lib/supabase/queries.ts`, same targeted single-row-query shape as the existing `getMostRecentTradeDate` (`select instrument ... order by date desc, created_at desc limit 1`) rather than fetching every trade to find one field.
- **`src/app/trades/new/page.tsx`**: `defaultInstrument` is now `mostRecentInstrument ?? settings?.default_instrument ?? DEFAULT_INSTRUMENT` — three-level fallback for a brand-new account (no trades → no settings row either, in the broken-trigger edge case) down to a hardcoded `EURUSD`.
- This only feeds the *fresh-form* default — Phase 4b's draft-restore path is untouched and still wins when a draft exists (`draft?.payload.values ?? { ..., instrument: defaultInstrument, ... }` in `new-trade-form.tsx`), so a restored draft's typed-but-unsaved instrument is never overridden by this change.

### 5. Emotion tags (On plan / Impatient / Chased entry / Early exit) are now user-editable

Moved from a hardcoded `LocaleStrings[]` constant to a per-user `settings.tag_presets text[]` column (migration `20260914130000_settings_tag_presets.sql`, seeded with the original four so existing users see no change until they edit). Editing lives in **Settings**, not inline on the trade form — a trader logging a trade in the moment wants to pick a tag, not administrate the list, and Settings is already where every other "what the form defaults to / offers" preference lives (P&L convention, default instrument, default session, R precision).

- **`Settings.tagPresets: string[]`** added to the domain type; `toSettings()` maps `row.tag_presets` straight through.
- **Settings screen** (`src/app/settings/settings-view.tsx`): new "Emotion tags" card with a `TagPresetsEditor` — each tag is its own inline-editable pill (a plain `<input>` sized to its content via the native `size` attribute, no separate "edit mode") with an `×` remove button, plus a dashed "add a tag" pill at the end (Enter or its own `+` button appends). Batched into the same "Save settings" action as every other field on the screen — nothing writes until Save, so a typo can be corrected before it's persisted. Server-side (`src/app/settings/actions.ts`) re-trims, drops empties, and de-duplicates independently of the client, and rejects an empty list outright ("Add at least one tag.") since an empty preset set would leave the trade form's tag row with nothing to offer.
- **Existing trades are untouched by design, not just by accident**: `trades.tags` already stored plain display strings with no foreign key to a preset list (the Phase 4a tag toggle always wrote `t(preset)` directly), so renaming or deleting a preset here can't reach into already-saved trades — there was nothing that needed migrating on that side. The one place this mattered for the *editing* UI: `TradeEditForm`'s tag row (`src/app/trades/[id]/trade-edit-form.tsx`) now shows the current presets **plus any tag already on that trade that isn't in the current preset list**, trailing after the presets — otherwise a trade tagged with something since renamed or deleted would have that tag silently un-removable (still saved, just invisible) rather than visible-and-optional.
- **`New trade` (desktop form + mobile quick-log wizard) and `Trade detail`'s edit form** all now take `tagPresets: string[]` as a prop instead of importing the old constant, threaded down from each route's own `getSettings()` call (`trades/new/page.tsx`, `trades/[id]/page.tsx`) with `DEFAULT_TAG_PRESETS` (the renamed, now-fallback-only version of the old constant, in `src/lib/labels.ts`) as the missing-settings-row fallback.

**Verified live**, all five items together, with a disposable test user (created and deleted via the Admin API — the browser session had the real user's saved credentials autofilled on `/login`, caught before submitting and swapped for the test account's own): set up a fresh account, logged three trades (EURUSD win, GBPUSD loss, GBPUSD break-even) through the real New trade form.
- **HTF pairing**: the field defaulted to "W → D" on a fresh form, and `find` confirmed the select's only two options are `M → W → D` / `W → D`. Saved trade's CRT-sequence chip read "W → D" on Trade detail.
- **Instrument memory**: after the EURUSD trade, a fresh `/trades/new` defaulted to EURUSD (the only trade so far); after switching to GBPUSD for the second trade, a fresh `/trades/new` defaulted to GBPUSD — confirmed via a zoomed screenshot of just the field, not inferred.
- **Win rate / Avg hold**: with 1 win / 1 loss / 1 BE logged, `/trades` read "Win rate 50%" (not 33%) with no "Avg hold" stat anywhere in the summary row; `/weekly-review` read "Win rate 50%" with sub-line "1 / 2 · 1 BE".
- **Emotion tags**: renamed "Impatient" → "FOMO", deleted "Chased entry", added "Revenge trade" in Settings, saved, and confirmed the New trade form's tag row picked up the exact new set ("On plan", "FOMO", "Early exit", "Revenge trade").
- **Mobile**: `resize_window` on the already-open tab didn't reliably take effect this session (a known limitation noted in an earlier phase above); opening a **new** tab and resizing *that* one before its first navigation worked reliably (390×844, Korean locale). Re-ran the same checks there: mobile Trade log showed the same 50%/no-Avg-hold result, the quick-log wizard's HTF pairing step defaulted to "W → D" and its Instrument step defaulted to "GBPUSD", the notes step showed the same four edited tags, and Settings' "감정 태그" card rendered and wrapped correctly at 390px width.
- Afterward, deleted the disposable user via the Admin API and confirmed via `psql`: `auth.users` back to exactly 1 row, the real account's 15 trades all still `m_w_d`/`w_d` with correct counts (12/3, unchanged from the migration verification above), and the real user's own `settings.tag_presets` still the untouched original four — the test session never wrote to real data.

`npx tsc --noEmit`, `pnpm lint`, `pnpm exec vitest run` (255 passed, one rewritten for the win-rate flip), and `pnpm build` all clean.

## Phase 6 — Instrument default bug fix, date input digit overflow, date memory

Three follow-ups from actual use of Phase 5's changes.

### 1. Bug: Settings' default Instrument had no effect — root cause was Phase 5's own instrument-memory feature

The user reported that changing the default ticker in Settings did nothing; the New trade form kept defaulting to `AUDUSD` regardless. Root cause: Phase 5's `getMostRecentTradeInstrument(accountId)` query unconditionally took priority over `settings.default_instrument` (`mostRecentInstrument ?? settings?.default_instrument ?? DEFAULT_INSTRUMENT`) — once at least one trade existed, the Settings field was permanently shadowed and dead, which is exactly what happened on the real account (its most recent trade happened to be AUDUSD). Confirmed directly: `select default_instrument from settings` on the live database showed whatever the user had actually set (`NZDUSD`), completely disconnected from what the form was showing.

**Fixed by making `settings.default_instrument` the single source of truth again, kept in sync automatically instead of being queried around**: `createTrade` (`src/app/trades/new/actions.ts`) now writes the trade's own instrument straight into `settings.default_instrument` right after a successful save (best-effort, same as the existing draft-cleanup calls below it). This keeps both halves of the original ask working at once — the field still "remembers what you logged last" (every save updates it), but explicitly changing it in Settings now sticks until the next trade is logged, rather than being invisibly overridden. `getMostRecentTradeInstrument` (the Phase 5 query) is deleted outright — there's no longer a second source of truth to reconcile. `src/app/trades/new/page.tsx` goes back to the simpler `settings?.default_instrument ?? DEFAULT_INSTRUMENT`.

Editing an existing trade's instrument (Trade detail's edit form) deliberately does **not** re-sync this — only newly logging a trade counts as "using" an instrument, matching the original request's own framing ("New trade 폼의 Instrument가 마지막에 입력한 값을 기억하게 해라").

### 2. Bug: the Date field's year segment accepted up to 6 digits

Reported directly: typing quickly into a plain `<input type="date">`'s year segment let it run past 4 digits. Root cause is a genuine native-input footgun, not app logic — without a `max` attribute, Chromium's date input accepts any year the `date` type's own spec allows (up to 275760, i.e. 6 digits), and none of this app's three trade-date inputs (New trade, the mobile quick-log wizard, Trade detail's edit form) had ever set `min`/`max`.

Added `MIN_TRADE_DATE = "1970-01-01"` / `MAX_TRADE_DATE = "2099-12-31"` (`src/lib/domain/dates.ts`) and applied both to all three inputs. Setting any 4-digit-year `max` is what actually caps the segment's own typing to 4 digits (a well-known trick for this exact native-input quirk) — the specific bound chosen is generous on both ends for a personal FX journal that may log old backtest history, not a tight business constraint. **Verified live**: typed `209999` into the year segment on a real form — it filled the year to `2099` (the configured max) and rolled the remaining digits into the day segment instead of accepting `209999` as a 6-digit year, confirming the cap actually holds rather than just trusting the attribute exists in the DOM.

Other `<input type="date">` fields in the app (account setup's start date, cash-movement date, the dashboard period-picker's custom range, the Trade log's date-range filter) were left untouched — they weren't part of the report, and two of them (cash-movement, period-picker) already cross-constrain their own min/max against each other or a real account/today bound, which is a different and already-adequate mitigation.

### 3. New trade's Date field now remembers the last-used date too, the same way Instrument does

Previously always defaulted to the real calendar "today" (`new Date().toISOString().slice(0, 10)`). Changed to the account's most-recently-logged trade's date, falling back to today only when the account has no trades yet — reusing `getMostRecentTradeDate`, a query that already existed for the Dashboard's own default period (docs/decisions.md § Phase 9 backtest follow-up), rather than adding a new one. Unlike Instrument, there's no Settings field for this (the user didn't ask for one), so there's no competing source of truth to shadow — a direct query is the whole fix, no write-through sync needed.

`NewTradeFormProps.today` renamed to `defaultDate` for clarity (it's genuinely not "today" once an account has trades) — confirmed safe to rename by checking every other reference to the old `today` prop name first; the "1R today" copy elsewhere in the same form is a separate, hardcoded UI string tied to `rValueToday` (the account's real current balance), not this prop.

**Verified live**, all three fixes together, with a fresh disposable test user (Admin API create/delete again; the browser again had the real user's login autofilled and was swapped out before submitting, same as every prior live-verification pass): set up a new account, logged one EURUSD trade dated 2026-09-10 (typed via the date input, not just passed as a value). Reopening `/trades/new` afterward showed the Date field pre-filled to `2026. 09. 10.` (not the real current date) and the Instrument field still `EURUSD`. Changed Settings' default instrument to `USDJPY` and saved; a fresh `/trades/new` immediately showed `USDJPY`, confirming the Settings field is no longer shadowed even with a trade already logged. Deleted the test user afterward and confirmed via `psql`: back to exactly 1 real user, and the real account's own `settings.default_instrument` (`NZDUSD`, whatever the user had actually set) was never touched by the test session — matching, and explaining, their original bug report.

`npx tsc --noEmit`, `pnpm lint`, `pnpm exec vitest run` (255 passed, no behavior change to any tested selector), and `pnpm build` all clean.
