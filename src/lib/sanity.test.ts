import { describe, expect, it } from "vitest";

// Phase 0 placeholder — proves the Vitest harness is wired up.
// Replaced by the real domain suite in src/lib/domain/*.test.ts (Phase 2).
describe("phase 0 harness", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
